# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Environment & Secrets
.env
.env.local
.env.*.local
*.PROD-BACKUP
*.key
*.pem

# IDE & OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# Testing
coverage/
test-results/
playwright-report/
blob-report/

# AIOS Local
.aios-core/local/
.claude/settings.local.json
.aios/install-log.txt

# Supabase local
supabase/.temp/

# claude-code-handoff (personal session state)
.claude/handoffs/

# Design Systems (local only — cada dev instala localmente)
design-systems/

# Database backups (real student data, keep local only)
supabase/_backups/
.vercel
.env*

# Session artifacts (Codex/Maestri runtime)
.codex-*.txt
.maestri/

# Janela de injeção (scripts/janela-de-injecao.mjs) — estado de máquina LOCAL.
# O sentinela declara que há defeito injetado de propósito nesta árvore agora.
# Se ele fosse versionado, viajaria para outra máquina travando commits lá por
# uma janela que já fechou aqui. O hook pre-commit barra até se forçado ao índice.
.janela-de-injecao.json
.janela-de-injecao-abandonos.log
