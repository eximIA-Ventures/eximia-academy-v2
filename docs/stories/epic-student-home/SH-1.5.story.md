# SH-1.5: Tabela "Meu ritmo" — reordenar, renomear+fração, linha Engajamento com rank real, coluna "Como estou", parágrafo-resumo

**Epic:** [EPIC-STUDENT-HOME](./EPIC-STUDENT-HOME.md)
**Status:** InReview
**Insumo obrigatório:** este documento substitui, para a tabela `ComparisonInsightsTable`/`StudentHomeCard`, a descrição genérica de "SH-1.5: Reancoragem de métricas na UI" do epic §10 — aquela entrada foi escrita antes da tabela existir no formato transposto atual (Hugo, iterações de 2026-07-14) e antes de SH-F.5 (fração de engajamento). Este documento é a fonte de verdade concreta do escopo, pedida diretamente pelo Hugo (fundador, dono do produto) via mockup visual.
**Depende de:** SH-1.4 (container `StudentHomeCard` já existe e está em produção, sem toggle de intenção — decisão posterior do Hugo removeu a manchete "Meu progresso", ver `student-home-card.tsx` linhas 1-26), SH-F.5 (campo `engagementMax` já existe em `StudentHomeSubject`, computado em `computeStudentComparison`, mas HOJE não é lido por `ComparisonInsightsTable` — a tabela atual não tem linha "Engajamento").
**Bloqueia:** nada identificado.
**Paralelizável:** NÃO recomendado com nenhuma outra story simultânea sobre `comparison-insights-table.tsx`, `student-home-indicators.ts`, `area-gestor.ts` ou `types/analytics.ts` — esta story reescreve a composição de linhas da tabela e estende `computeStudentComparison`.

---

## Story

**As a** aluno olhando o card "Meu ritmo" na minha home,
**I want** que a tabela "Visão detalhada" mostre minhas 5 métricas na ordem certa, com os rótulos certos, frações honestas nas métricas que têm denominador, uma linha de Engajamento comparável em escore absoluto, uma coluna "Como estou" com frases mais completas que o chip atual, e um parágrafo-resumo pessoal logo abaixo,
**so that** eu entenda meu ritmo de forma mais rica e precisa do que hoje, incluindo saber com certeza (nunca por aproximação) se sou o aluno mais engajado da turma.

## Contexto (Dev Notes)

**IMPORTANTE — o CODE_MAP fornecido pelo Hugo junto com o pedido está desatualizado em pontos estruturais.** O app evoluiu (iterações de 2026-07-14 documentadas no cabeçalho de `comparison-insights-table.tsx`) para um formato TRANSPOSTO com coluna "Leitura" (chip tonal curto), diferente do estado "ESTADO ATUAL" descrito no pedido original (que mencionou 4 linhas com uma coluna "LEITURA" chip curto — isso na verdade já é exatamente o código real hoje, então o pedido do Hugo bateu com o código; o que diverge é o texto de "screenshot" citando nomes de arquivo/estrutura que já mudaram). Este documento usa os anchors REAIS, verificados em 2026-07-18.

### Estado real hoje (verificado em código)

- **Arquivo da tabela:** `apps/web/src/components/analytics/comparison-insights-table.tsx`. Formato transposto, 4 linhas fixas (`buildRows`, linha 178): `progress` ("Progresso"), `sessions` ("Sessões concluídas"), `reflections` ("Reflexões"), `lastAccess` ("Último acesso"). 4 colunas: Indicador | `{subjectColumnLabel}` ("Eu (Nome)"/"Você") | Turma | Leitura (chip tonal curto: `LEITURA_COPY` win/tie/behind por linha, linha 95).
- **Container:** `apps/web/src/components/analytics/student-home-card.tsx`. Título "Meu ritmo" (minúsculo — MANTER, não copiar o "Meu Ritmo" do mockup do Hugo), subtítulo "Como estou em relação à turma nos últimos 30 dias." (MANTER texto atual, não é escopo desta story mudar). Toggle único "Visão detalhada"/"Gráficos" (não há mais toggle de intenção "progresso"/"comparar" — SH-1.4 original prev增 isso, mas Hugo removeu depois; não reabrir).
- **Tipos:** `apps/web/src/types/analytics.ts` — `StudentHomeSubject` (linha 642: `lastAccessDays`, `ritmoDisplay?`, `lastCompletedLabel?`, `progressPct`, `engagement`, `interactions`, `reflections`, `engagementMax?` já existe, linha 670), `StudentHomeReference` (linha 674: `lastAccessAvgDays`, `ritmoEmDiaPct`, `progressAvgPct`, `engagementAvg`, `interactionsAvg`, `reflectionsAvg`), `StudentHomeIndicators` (linha 693: `{ subject, reference }`).
- **Lógica de indicadores:** `apps/web/src/lib/analytics/student-home-indicators.ts`. `buildStudentHomeIndicators` (linha 135) já recebe `engagementMax?: number` como parâmetro (linha 144) e já o expõe em `subject.engagementMax` (linha 310) — infraestrutura de SH-F.5, **mas a tabela hoje NÃO lê esse campo** (não há linha "Engajamento" em `buildRows`).
- **N (teto do Você), já implementado (SH-F.5):** `computeEngagementMax(trailChapterCount, reflectionPossibleSlides)` (linha 89) = `trailChapterCount * 2 + reflectionPossibleSlides`. Helpers `trailChapterIdsOf` (linha 62) e `countReflectionPossibleSlides` (linha 84) também já existem e já são chamados em `computeStudentComparison`.
- **Fetch/orquestração:** `apps/web/src/lib/analytics/area-gestor.ts`, função `computeStudentComparison` (linha 1332). Já calcula `engagementMax` (linha 1406-1409) via `trailChapterIdsOf` + scan de `chapter_slides` da trilha do aluno (fresco, por request, fora do `OrgReference` cacheado) e passa para `buildStudentHomeIndicators` (linha 1468). `OrgReference` (interface, linha 1184) é o cache org-wide (SH-F.3/SH-F.5): contém `chapterRows`, `activeCourseIds`, `orgStudentIds`, `orgSessionRows`, `orgReflectionRows`, `orgEnrollmentRows`, `orgBlock`, etc. — **zero dado por-aluno cacheado**, ver guarda em `loadOrgReference` (linha 1224).
- **Fórmula de engajamento (intocada, preservar):** `engagementOf(id) = interactionsOf(id) * 2 + reflectionsOf(id)` (`student-home-indicators.ts:273`), `interactionsOf = completedByStudent` (sessões concluídas), `reflectionsOf = reflectionsByStudent` (contagem de `slide_reflections`). `reference.engagementAvg` é derivado dos dois `Avg` arredondados (linha 352, comentário explica a identidade matemática).
- **Winner/leitura:** `winnerOf(subject, reference, direction)` (linha 58) e `leituraFor(key, subject, reference, direction)` (linha 116) são funções puras exportadas, já testadas.

### O que este pedido pede DE FATO (mockup do Hugo), tradução para os anchors reais

Hugo pediu 6 mudanças concretas na tabela + 1 novidade (parágrafo-resumo). Nenhuma delas exige mudar a fórmula de engajamento, `winnerOf`, ou as APIs de `toMetricBar`/`computeMetricBlock` (que pertencem a outro caminho, o card do gestor — fora do blast radius).

1. **Reordenar as linhas** de `buildRows` para: Última atividade → Progresso - conclusão → Interações realizadas → Reflexões realizadas → Engajamento (nova).
2. **Renomear 3 linhas existentes** (label apenas, `RowKey` interno pode manter os nomes atuais ou ser expandido — decisão de implementação livre, desde que o rótulo visível mude):
   - `lastAccess`: "Último acesso" → **"Última atividade"**.
   - `progress`: "Progresso" → **"Progresso - conclusão"**.
   - `sessions`: "Sessões concluídas" → **"Interações realizadas"**.
   - `reflections`: "Reflexões" → **"Reflexões realizadas"**.
3. **Fração X/Y em 2 linhas** ("Interações realizadas" e "Reflexões realizadas"), com denominadores DISTINTOS do `engagementMax` (que soma os dois com pesos ×2/×1 — não é o denominador de cada linha isolada):
   - Denominador de "Interações realizadas" = `trailChapterCount` (o número de capítulos da trilha do aluno — SEM o ×2 que `computeEngagementMax` aplica).
   - Denominador de "Reflexões realizadas" = `reflectionPossibleSlides` (o número de slides-com-reflexão-possível da trilha do aluno).
   - Ambos os denominadores JÁ SÃO CALCULADOS hoje dentro de `computeStudentComparison` (`trailChapterIds.length` e `countReflectionPossibleSlides(trailSlideRows)`, linhas 1406-1409) — eles hoje só alimentam a SOMA `engagementMax`; esta story precisa preservá-los como valores próprios (não apenas a soma) para propagar cada um separadamente.
4. **Nova linha "Engajamento"**: score absoluto Você/Turma (`subject.engagement` / `reference.engagementAvg`, já existem), **SEM fração nesta linha** (SH-F.5 fez a fração em `engagementMax`, mas o mockup do Hugo pede o absoluto aqui — a fração de SH-F.5 fica reservada para a leitura "Como estou" desta linha, ver item 6 abaixo, não para o valor numérico da célula).
5. **Coluna "Leitura" → "Como estou"**: mesma mecânica (`winnerOf` + dicionário por linha + tom win/tie/behind), mas com frases MAIS LONGAS (prefixo "…", estilo do mockup: "… ativo acima da média", "… ritmo acima da média", "… acima da média", "… 1º da turma – Parabéns!"). O componente `LeituraChip`/`LEITURA_COPY` é reaproveitado e renomeado ou estendido — decisão de implementação livre — mas o CONTRATO de tom (win=reforço verde, tie=neutro, behind=convite nunca punitivo) É PRESERVADO. Renomear a variável/testid interno é aceitável; mudar o header da coluna de "Leitura" para "Como estou" é obrigatório.
6. **Parágrafo-resumo** abaixo da tabela: função PURA e determinística (sem LLM) que compõe uma frase personalizada a partir dos indicadores já calculados, formato do mockup: `"Parabéns {primeiroNome}, você é o aluno mais engajado da turma, seu ritmo está acima da média, você se mantém ativo em bom ritmo, com atividades recentes. Sua oportunidade de melhoria é concluir as reflexões e interações pendentes."` — a abertura elogiosa e a frase final de oportunidade de melhoria MUDAM de acordo com o estado real do aluno (ver AC8/AC9).

### REGRA DE NEGÓCIO CRÍTICA — rank real de engajamento (inegociável, Art. IV No Invention aplicado ao inverso: não inventar um "1º lugar" que não existe)

A frase **"1º da turma – Parabéns!"** na coluna "Como estou" da linha Engajamento, e a abertura **"você é o aluno mais engajado da turma"** no parágrafo-resumo, só podem aparecer quando uma computação REAL no backend confirmar que o aluno é #1 em engajamento entre os alunos comparáveis da organização. É proibido hardcodear ou aproximar essa afirmação — seria uma alegação falsa a um usuário real de produção.

- **O rank é a posição do aluno** ao ordenar TODOS os alunos ativos comparáveis da org por `engagement` (mesma fórmula `interactions*2 + reflections`) em ordem decrescente. Rank 1 = maior engajamento da organização.
- **Calculado no backend** (`computeStudentComparison`/`area-gestor.ts`), nunca no cliente — o cliente não tem acesso aos dados de outros alunos.
- **NUNCA expor identidade ou score de outros alunos** ao cliente — mesma postura LGPD já fixada no epic (`EPIC-STUDENT-HOME.md` §3 premissa "só o aluno logado vê sua própria comparação"; ver também a guarda "aluno nunca cacheado" de SH-F.3/SH-F.5). O único dado que sai do backend para o cliente é um booleano/rank do PRÓPRIO aluno (`isTopEngagement: boolean` ou equivalente), nunca a lista ordenada nem os scores alheios.
- **"Alunos comparáveis"** = a mesma população usada como referência hoje: `orgRef.orgStudentIds` (role=student, tenant-scoped, sem filtro de área — M2, já documentado em `OrgReference`). Não inventar uma nova noção de "comparável".
- **Quando o aluno NÃO for #1:** cai no fallback já existente da leitura da linha Engajamento (win/tie/behind conforme `winnerOf(engagement, engagementAvg, "higher")`), e o parágrafo-resumo usa uma abertura sem a alegação de 1º lugar (ver AC9). Nunca mostrar "1º da turma" fantasiado.
- **Empate no topo (2+ alunos com o mesmo engajamento máximo):** tratar como NÃO-#1 exclusivo é a escolha mais segura por padrão (evita afirmar "você é O mais engajado" quando há empate) — a story define isto como AC12, mas está aberta para o Contrato (@po) endossar ou ajustar essa regra de desempate na validação.

## Acceptance Criteria

- [x] **AC1 (ordem exata das linhas):** `buildRows` (ou equivalente) retorna as 5 linhas nesta ordem exata: 1) Última atividade, 2) Progresso - conclusão, 3) Interações realizadas, 4) Reflexões realizadas, 5) Engajamento. Teste asserta a ordem via `rows.map(r => r.label)` ou os `data-testid` das `<tr>` na ordem do DOM.
- [x] **AC2 (labels exatos):** os 5 rótulos visíveis na coluna Indicador são exatamente: "Última atividade", "Progresso - conclusão", "Interações realizadas", "Reflexões realizadas", "Engajamento". Teste de snapshot/texto por `data-testid`.
- [x] **AC3 (fração em Interações realizadas):** a célula "Você" da linha Interações realizadas renderiza `"{interactions}/{trailChapterCount}"` (ex.: `"7/10"`) quando o denominador está disponível; degrada para `"{interactions}"` (sem fração) se o denominador for `0`/indisponível (trilha vazia), sem crash. A célula "Turma" continua absoluta (`interactionsAvg`, sem fração — decisão explícita, espelha o precedente de SH-F.5 onde a Média fica absoluta).
- [x] **AC4 (fração em Reflexões realizadas):** mesma mecânica do AC3, com denominador `reflectionPossibleSlides` (ex.: `"41/50"`). Célula "Turma" absoluta (`reflectionsAvg`).
- [x] **AC5 (linha Engajamento, absoluto, sem fração):** a nova linha renderiza `subject.engagement` e `reference.engagementAvg` como números absolutos (SEM `/N`) nas duas colunas de valor — a fração `engagementMax` de SH-F.5 NÃO é usada como conteúdo da célula numérica nesta linha (é usada apenas na composição da leitura "Como estou", conforme AC7).
- [x] **AC6 (coluna renomeada):** o header da 4ª coluna muda de "Leitura" para "Como estou". Frases mais longas com prefixo "…" para cada uma das 5 linhas, com tom (win reforço verde / tie neutro / behind convite nunca punitivo) preservado do padrão atual (`LEITURA_COPY`/`LEITURA_CHIP`). Sugestão de copy por linha (implementação pode ajustar redação mantendo o tom): "… ativo acima da média" (Última atividade), "… ritmo acima da média" (Progresso), "… acima da média" (Interações/Reflexões), e a Engajamento com a regra do AC7/AC8.
- [x] **AC7 (rank real — bloqueante):** o backend (`computeStudentComparison`) calcula a posição real do aluno ao ordenar `orgStudentIds` por `engagementOf(id)` decrescente, e expõe um sinal aditivo e opcional em `StudentHomeSubject` (ex.: `isTopEngagement?: boolean`, ou `engagementRank?: number` — decisão de shape é do dev/@po na validação, mas DEVE ser booleano-consumível pelo cliente sem carregar identidade/score de terceiros). A leitura "Como estou" da linha Engajamento mostra **"… 1º da turma – Parabéns!"** SOMENTE quando esse sinal confirma rank real = 1 (sem empate, ver AC12). Teste cobre: aluno #1 real → frase aparece; aluno não-#1 (mesmo com `engagement > engagementAvg`) → frase NÃO aparece, cai no fallback padrão (win/tie/behind de `winnerOf`).
- [x] **AC8 (parágrafo-resumo, função pura, determinística):** nova função pura (ex.: `buildRitmoSummary(indicators, studentFirstName, rankSignal)`) compõe o parágrafo a partir SOMENTE dos indicadores já calculados (nenhuma chamada a LLM, nenhum I/O). Sem RNG, mesma entrada sempre produz a mesma saída (testável por igualdade exata).
- [x] **AC9 (parágrafo, 3 cenários testados, given/when/then):**
  - **Cenário A — aluno #1 real:** *Given* rank real = 1 (sem empate); *Then* a abertura do parágrafo é uma variação de "Parabéns {primeiroNome}, você é o aluno mais engajado da turma" — usa o rank real, nunca hardcoded.
  - **Cenário B — aluno acima da média mas NÃO #1:** *Given* `engagement > engagementAvg` mas rank ≠ 1; *Then* a abertura NÃO contém "mais engajado da turma" nem "1º da turma" — usa uma abertura alternativa que reconhece o desempenho acima da média sem a alegação de 1º lugar (ex.: "Parabéns {primeiroNome}, seu engajamento está acima da média da turma.").
  - **Cenário C — aluno abaixo da média em pelo menos 1 métrica:** *Given* pelo menos uma das métricas (progresso, interações, reflexões, engajamento, atividade recente) está com leitura `behind` (`winnerOf` = `"reference"`); *Then* o parágrafo inclui uma frase de "oportunidade de melhoria" que aponta DINAMICAMENTE para a(s) métrica(s) onde o aluno está atrás — nunca uma métrica fixa/hardcoded, nunca "reflexões e interações" se o aluno na verdade está atrás em progresso e atividade. Se NENHUMA métrica está atrás (aluno vence ou empata em todas), a frase final é positiva (sem "oportunidade de melhoria" forçada) — não inventar um ponto fraco que não existe.
- [x] **AC10 (frações honestas, sem crash):** com denominador `0` ou dado ausente (aluno sem trilha matriculada, caso degenerado), as células de fração (AC3/AC4) degradam para o valor absoluto sem `NaN`, sem `Infinity`, sem crash de render — mesmo padrão de degradação graciosa já usado em `engagementMax` (SH-F.5, `subjectNode: s.engagementMax != null ? ... : String(s.engagement)`, no precedente de `student-home-indicators.ts`).
- [x] **AC11 (sem regressão nas invariantes do epic):** `winnerOf`/`leituraFor` continuam funções puras testadas isoladamente; nenhuma assinatura de `computeMetricBlock`/`toMetricBar` muda (fora do blast radius desta story); suítes existentes `student-comparison-scale.test.ts` e `route-student-view.test.ts` seguem verdes sem modificação. "Abaixo" continua NEUTRO/acionável, nunca vermelho/punitivo (grep anti-vermelho aplicado às novas leituras também).
- [x] **AC12 (regra de desempate no topo, sujeita a validação do @po):** se 2+ alunos compartilham o maior `engagement` da organização, NENHUM deles recebe `isTopEngagement = true` por padrão (exclusividade estrita do "1º lugar" evita a alegação "você é O mais engajado" quando há empate real). Documentar esta escolha explicitamente no código (comentário) para que o @po possa endossar ou pedir ajuste na validação.
- [x] **AC13 (mantém decisões de produto já fixadas no epic — NÃO reabrir):** referência continua sendo a organização inteira (`orgStudentIds`, sem filtro de área — M2); só o aluno logado vê sua própria comparação (o backend nunca expõe identidade/score de colegas, nem na resposta da API nem em log); "Última atividade" continua mostrando a PENÚLTIMA visita (reaproveita `subjectLastAccessDays`/AJUSTE 2, não reabrir essa lógica); título do card continua **"Meu ritmo"** (minúsculo) — o mockup do Hugo escreve "Meu Ritmo" com R maiúsculo, mas isto NÃO é escopo desta story (é convenção de capitalização já assentada no app), não alterar o título nem o subtítulo do `CardHeader` em `student-home-card.tsx`.

## Tasks

- [x] 1. First-move: rodar a suíte do módulo analytics (`student-home-indicators`, `comparison-insights-table`, `area-gestor`) e confirmar VERDE antes de editar.
- [x] 2. Em `area-gestor.ts` (`computeStudentComparison`): preservar `trailChapterIds.length` e `countReflectionPossibleSlides(trailSlideRows)` como valores próprios (não só a soma consumida por `computeEngagementMax`), e computar o rank real do aluno ordenando `orgRef.orgStudentIds` por `engagementOf`/`engagement` decrescente (reusar a lógica de agregação já presente em `buildStudentHomeIndicators`/`student-home-indicators.ts`, sem duplicar o cálculo de `engagementOf` — considerar expor um helper pequeno e puro, ex. em `student-home-indicators.ts`, para computar o rank a partir dos mesmos mapas já construídos ali).
- [x] 3. Em `types/analytics.ts`: estender `StudentHomeSubject` com os novos campos aditivos e opcionais necessários (trail chapter count, reflection-possible slides, sinal de rank/top-engagement) — nomes exatos são decisão de implementação, mas devem ser opcionais (`?`) para não quebrar consumidores existentes.
- [x] 4. Em `student-home-indicators.ts`: propagar os novos campos (denominadores + sinal de rank) de `buildStudentHomeIndicators` para `subject`, seguindo o padrão aditivo já usado por `engagementMax` (SH-F.5).
- [x] 5. Em `comparison-insights-table.tsx`: reordenar/renomear `buildRows` (AC1/AC2), adicionar frações em Interações/Reflexões (AC3/AC4), adicionar a linha Engajamento absoluta (AC5), renomear a coluna "Leitura" para "Como estou" com frases mais longas (AC6) incluindo a regra do rank real (AC7).
- [x] 6. Nova função pura `buildRitmoSummary` (local sugerido: `comparison-insights-table.tsx` ou `student-comparison-scale.ts`, decisão de implementação) implementando os 3 cenários do AC9, chamada a partir de `student-home-card.tsx` e renderizada como parágrafo abaixo da tabela.
- [x] 7. Guardar contra regressão de LGPD: revisar o payload retornado ao cliente (rota `manager-groups?view=student` ou equivalente) para confirmar que NENHUM dado de outro aluno (id, nome, score) vaza junto com o sinal de rank.
- [x] 8. Escrever/estender os testes: AC1/AC2 (ordem+labels), AC3/AC4/AC10 (frações + degradação), AC5 (Engajamento absoluto), AC6/AC7 (Como estou + rank real, incluindo o caso "acima da média mas não #1"), AC8/AC9 (parágrafo, 3 cenários), AC12 (empate no topo).
- [x] 9. Rodar a suíte completa do módulo + `pnpm --filter @eximia/web typecheck` e confirmar VERDE ao final (AC11).

## Complexidade & Riscos

- **Complexidade:** L (large). Reescreve a composição de linhas/colunas de um componente em produção, estende o backend (`computeStudentComparison`) com uma computação nova (rank real sobre toda a população org), e introduz uma função de composição de texto com regras condicionais não-triviais (3 cenários).
- **Riscos:**
  - R1 (alto, é a razão de existir do AC7): calcular o rank de forma aproximada/hardcoded em vez de real. Mitigação: AC7 explícito + teste do cenário "acima da média mas não #1" que provaria a diferença entre "acima da média" (fácil, já existe) e "#1 real" (novo, exige ordenar toda a população).
  - R2 (médio): vazar identidade/score de outros alunos no payload ao expor o sinal de rank. Mitigação: Task 7 (revisão explícita do payload) + AC13 (reforça a postura LGPD já fixada no epic).
  - R3 (médio): parágrafo-resumo hardcoded ou com métrica fixa de "oportunidade de melhoria" que não reflete o estado real do aluno. Mitigação: AC9 cenário C exige que a métrica seja escolhida dinamicamente a partir de `winnerOf`/leituras reais, testado com fixtures onde a métrica-fraca varia.
  - R4 (baixo): confundir o denominador de `engagementMax` (soma ponderada ×2/×1) com os denominadores por linha (`trailChapterCount` puro, `reflectionPossibleSlides` puro). Mitigação: Dev Notes explicita a distinção; AC3/AC4 travam os valores exatos por fixture.
  - R5 (baixo): empate no topo gerando alegação incorreta de "1º lugar" para múltiplos alunos simultaneamente. Mitigação: AC12 define a regra padrão (nenhum vence em empate) e a marca como sujeita à validação do @po.

## Dev Notes

- **Pontos de entrada reais** (todos verificados em código, 2026-07-18): `apps/web/src/components/analytics/comparison-insights-table.tsx` (tabela), `apps/web/src/components/analytics/student-home-card.tsx` (container, título/subtítulo, local de renderização do novo parágrafo), `apps/web/src/lib/analytics/student-home-indicators.ts` (`buildStudentHomeIndicators`, propagação de campos), `apps/web/src/lib/analytics/area-gestor.ts` (`computeStudentComparison`, cálculo do rank real e dos denominadores por linha), `apps/web/src/types/analytics.ts` (`StudentHomeSubject`/`StudentHomeReference`/`StudentHomeIndicators`).
- **Reusar, não reinventar:** `engagementOf`/`interactionsOf`/`reflectionsOf` já existem dentro de `buildStudentHomeIndicators` (student-home-indicators.ts, linhas 271-273) — o cálculo do rank real deve reusar a MESMA fórmula de engajamento sobre os MESMOS mapas já construídos ali (`completedByStudent`, `reflectionsByStudent`), não recalcular do zero em `area-gestor.ts`.
- **Precedente de degradação graciosa (SH-F.5):** `subjectNode: s.engagementMax != null ? \`${s.engagement} de ${s.engagementMax}\` : String(s.engagement)` em `comparison-insights-table.tsx` é o padrão a replicar para as novas frações (AC3/AC4/AC10) — campo opcional, ausência degrada ao absoluto, nunca quebra.
- **NÃO alterar** `winnerOf`, `toMetricBar`, `computeMetricBlock`, o card do gestor (`student-insights-table.tsx`), ou o toggle "Visão detalhada"/"Gráficos" existente em `student-home-card.tsx` — fora do escopo desta story.
- **Regra da casa:** copy nova sem travessão (—), usar vírgula (`.claude/rules` do ecossistema Hugo, aplicável a toda escrita gerada).

## Testing

```bash
cd /Users/hugocapitelli/Dev/eximia/eximia-academy-v2
pnpm --filter @eximia/web test -- student-home-indicators comparison-insights-table area-gestor   # first-move: verde ANTES
# ... implementar ...
pnpm --filter @eximia/web test -- comparison-insights-table   # AC1-AC7, AC10-AC12
pnpm --filter @eximia/web test -- student-home-indicators     # AC3/AC4/AC7 (propagação dos campos)
pnpm --filter @eximia/web test -- area-gestor                 # AC7 (rank real), AC13 (payload sem vazamento)
pnpm --filter @eximia/web test -- student-comparison-scale route-student-view   # AC11, intactos
pnpm --filter @eximia/web typecheck
pnpm --filter @eximia/web lint
```

Verificação visual: `pnpm --filter @eximia/web dev -- -p 3002`, abrir `http://localhost:3002/dashboard` como aluno e conferir a ordem/labels/frações/coluna "Como estou"/parágrafo; idealmente testar com pelo menos 2 contas (uma #1 real da org, uma não-#1) para ver as duas aberturas do parágrafo.

## Change Log

| Data | Mudança | Autor |
|------|---------|-------|
| 2026-07-18 | Story criada a partir de pedido direto do Hugo (fundador, mockup visual) para a tabela "Visão detalhada" do card "Meu ritmo". Anchors verificados em código real (2026-07-18): tabela já no formato transposto com coluna "Leitura", `engagementMax` já existe como infra de SH-F.5 mas não renderizado nesta tabela, `trailChapterIdsOf`/`countReflectionPossibleSlides`/`computeEngagementMax` já existem em `student-home-indicators.ts`. Regra do rank real (AC7) cravada como bloqueante — nenhuma alegação de "1º da turma" sem confirmação real do backend. | Roteiro (@sm) |
| 2026-07-18 | Implementação completa (AC1-AC13). Tabela redesenhada para 5 linhas (Última atividade, Progresso - conclusão, Interações realizadas, Reflexões realizadas, Engajamento) com reorder+rename de `buildRows`; fração graciosa X/Y só no lado Você de Interações/Reflexões (`formatFraction`, degrada ao absoluto sem NaN/crash); coluna "Leitura" renomeada para "Como estou" com frases longas prefixo "…". Rank real de engajamento (AC7) computado no backend por `buildStudentHomeIndicators` reusando os mesmos mapas de agregação sobre `orgStudentIds` (sem duplicar `engagementOf`), exposto como `subject.isTopEngagement` (booleano tie-safe, estrito, sem PII de terceiros); "1º da turma – Parabéns!" só aparece com rank real = 1. Parágrafo-resumo pela nova função pura determinística `buildRitmoSummary` (`ritmo-summary.ts`): abertura condicionada ao rank real, oportunidade de melhoria dinâmica (`behindMetricsOf`, nunca métrica hardcoded). Campos aditivos/opcionais em `StudentHomeSubject` (`interactionsMax?`, `reflectionsMax?`, `isTopEngagement?`). Sem travessão na copy nova. typecheck exit 0; 102/102 testes verdes nos 4 arquivos do domínio (ritmo-summary 11, comparison-insights-table 29, student-home-indicators 37, area-gestor 25). | Dex (@dev) |
| 2026-07-18 (Round 2) | Ajustes de refinamento visual a partir de feedback direto do Hugo olhando o app rodando localmente. **(1)** Pills "Como estou" perderam o prefixo de elipse "… " (frases agora começam limpas, sem a reticência de continuação). **(2)** Fração "X/Y" passou a aparecer nos DOIS lados (célula Você E célula Turma) das linhas "Interações realizadas" e "Reflexões realizadas" — antes só o lado Você trazia fração; a Turma passa a mostrar `{média}/{denominador}` com o mesmo denominador da trilha. **(3)** Linha "Engajamento" ficou ASSIMÉTRICA por decisão explícita do Hugo: a célula Você passou a exibir RANKING no formato "Xº de Y" (ranking de competição padrão — empate compartilha a mesma posição) em vez da pontuação bruta; a célula Turma passou a exibir a fração de pontos "X/Y". O vencedor da linha, a cor e a leitura "Como estou" continuam calculados sobre os SCORES BRUTOS (`subject.engagement`/`reference.engagementAvg`), sem mudança de lógica — só o texto exibido nas células mudou. **(4)** Parágrafo-resumo (`buildRitmoSummary`) saiu de texto simples e passou a viver num painel escuro em destaque, com o CTA "Continuar agora" ancorado no canto do MESMO painel, sem o rótulo "Próximo passo: …". O `NextStepBar` compartilhado ficou 100% intocado nas outras 2 chamadas em `student-comparison-view.tsx` (o CTA em destaque é local ao painel do resumo, não o componente compartilhado). typecheck exit 0; lint limpo; 226/226 testes verdes. 9 arquivos tocados (5 de código + 4 de teste): `comparison-insights-table.tsx`, `student-home-card.tsx`, `area-gestor.ts`, `student-home-indicators.ts`, `types/analytics.ts`, `comparison-insights-table.test.tsx`, `student-home-card.test.tsx`, `area-gestor.test.ts`, `student-home-indicators.test.ts`. | Dex (@dev) |
| 2026-07-18 (Round 3) | Ajustes de severidade e legibilidade a partir de feedback direto do Hugo olhando o app rodando localmente. **(1) Pontuação bruta sob o ranking (linha Engajamento):** a célula Você da linha "Engajamento" ganhou uma 2ª linha (legenda pequena, muted) com a pontuação bruta no formato "Você fez {N} pontos", renderizada ABAIXO do ranking "Xº de Y" que a Round 2 já exibia. A adição é EXCLUSIVA da célula Você da linha Engajamento — a célula Turma e as demais linhas permanecem idênticas. **(2) Severidade amarelo/vermelho quando atrás:** o chip "Como estou" E o pill de valor da célula Você passam a ficar amarelos (severidade `mild`) ou vermelhos (severidade `severe`) quando o aluno está abaixo da média naquela linha; antes existia apenas o tom único "cerrado/behind" sem gradação. Nova função pura `behindSeverityOf` (direction-aware) com threshold nomeado `SEVERE_BEHIND_THRESHOLD = 0.3`: quando `winnerOf = "reference"` (aluno atrás), o gap relativo `> SEVERE_BEHIND_THRESHOLD` classifica como `severe` (vermelho), senão `mild` (amarelo); a Turma NUNCA destaca; empate continua neutro. **Esta mudança REVERTE explicitamente a decisão "Jamais vermelho" documentada anteriormente no cabeçalho de `comparison-insights-table.tsx`** — o comentário histórico foi PRESERVADO (não apagado), com um bloco novo datado explicando a reversão deliberada solicitada pelo Hugo nesta rodada. typecheck exit 0; 239/239 testes verdes (baseline era 226 antes da Round 3). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 4) | Botão acionável por linha quando o aluno está atrás da média, a partir de feedback direto do Hugo olhando o app rodando localmente. **(1) Botão compacto ao lado do chip "Como estou":** cada linha da tabela ganhou um botão acionável compacto renderizado ao lado do chip "Como estou", que aparece SOMENTE quando o aluno está atrás da média naquela linha (mesma condição de severidade da Round 3, `mild` ou `severe` via `winnerOf = "reference"`) — quando o aluno vence ou empata, nenhum botão aparece. **(2) Label por linha:** o texto do botão é específico da métrica: "Retomar atividade" (Última atividade), "Continuar sessão" (Progresso), "Fazer uma interação" (Interações), "Registrar uma reflexão" (Reflexões), "Continuar agora" (Engajamento). **(3) Deep-link único (decisão pragmática documentada):** todos os botões apontam para o MESMO `continueHref` já existente no app — NÃO há deep-link para reflexão/interação/atividade específica hoje, então o botão leva o aluno ao ponto de continuação geral. A decisão pragmática está documentada em comentário no código: quando o app ganhar deep-links granulares por métrica, cada label passa a apontar para o alvo específico sem mudar a estrutura do botão. **(4) Prop aditivo seguro:** novo prop `continueHref` em `ComparisonInsightsTable`, opcional com default seguro (`DEFAULT_CONTINUE_HREF`) para não quebrar os usos existentes em teste; `student-home-card.tsx` passa o valor real do app. typecheck exit 0; 248/248 testes verdes (baseline era 239 antes da Round 4). 4 arquivos tocados: `comparison-insights-table.tsx`, `student-home-card.tsx`, `comparison-insights-table.test.tsx`, `student-home-card.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 5) | Simetria de legenda de pontuação na linha Engajamento, a partir de feedback direto do Hugo olhando o app rodando localmente. **Problema:** a célula Turma da linha "Engajamento" mostrava só o número cru da fração de pontos ("13/57", pontos-feitos/pontos-possíveis), sem legenda em texto, enquanto a célula Você já tinha (desde a Round 3) a legenda muted "Você fez {N} pontos" logo abaixo do ranking, o que deixava as duas células assimétricas na leitura. **Correção:** adicionada uma 2ª linha (legenda pequena, muted, mesmo estilo visual `text-xs text-text-muted` da legenda Você) na célula Turma da linha Engajamento com o texto "Turma fez {N} pontos, em média", usando `indicators.reference.engagementAvg` (a MESMA fonte que já alimenta o numerador da fração "X/Y" exibida nessa célula — nenhum dado novo, só a legenda em texto do valor que já estava sendo mostrado). A fração "X/Y" foi PRESERVADA, a legenda é aditiva. Adição EXCLUSIVA da célula Turma da linha Engajamento — célula Você e todas as demais linhas permanecem idênticas; nenhuma mudança na lógica de vencedor/cor/leitura (continua sobre os scores brutos). typecheck exit 0; 251/251 testes verdes (baseline era 248 antes da Round 5). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 6) | Simplificações de legibilidade da tabela "Meu ritmo", todas a partir de feedback direto do Hugo olhando o app rodando localmente. **(1) Engajamento/Turma sem denominador confuso:** a célula Turma da linha "Engajamento" mostrava a fração de pontos "{N}/57" e o "57" gerou confusão real do Hugo ("o que é esse 57?"), pois o denominador de pontos possíveis da turma não tem leitura óbvia. Passou a exibir só o valor absoluto `String(r.engagementAvg)` — a média de pontos da turma, sem fração. **(2) Botão acionável agora UNIVERSAL:** o `ActionButton` (introduzido na Round 4) deixou de ser condicional a "aluno atrás da média" e passou a aparecer em TODAS as 5 linhas, sempre, como CTA de "continue melhorando" — pedido explícito do Hugo: "mesmo para o Rinaldo, tem que ter os botões para melhorar ainda mais a performance dele". O aluno que já vence a média também ganha o caminho de ação para evoluir mais. Label por linha e `continueHref` únicos permanecem os da Round 4. **(3) Ranking sem total (célula Você de Engajamento):** `formatRank` passou a retornar só "{rank}º" (ex.: "11º") em vez de "{rank}º de {total}" (ex.: "11º de 46") — o "de 46" confundia e não agregava; a posição sozinha comunica o ranking. **(4) Turma/Engajamento consolidada numa única frase:** a célula Turma da linha Engajamento foi unificada numa única linha de texto ("a turma fez, em média, {N} pontos"), removendo o número bruto solto que ficava visualmente separado da legenda (herança da Round 5) — agora é só a frase, sem número duplicado acima dela. Nenhuma mudança na lógica de vencedor/cor/leitura (segue sobre os scores brutos, `winnerOf`/`leituraFor` intocados). typecheck exit 0; 252/252 testes verdes (baseline era 251 antes da Round 6). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 10) | Ícones SEMÂNTICOS por ação + diferenciação botão↔chip, a partir de feedback direto do Hugo olhando o app com os Rounds 8/9 já aplicados: *"precisamos melhorar o visual dos botões agora, ta tudo muito igual. precisamos dos botões com alguns ícones e etc"*. Dois problemas, duas correções. **(1) Ícone genérico repetido:** até a Round 9 os 5 botões de ação terminavam todos no MESMO `ArrowRight` genérico, ficando visualmente idênticos entre si. Novo mapa `ACTION_ICON: Record<RowKey, LucideIcon>` (paralelo a `ACTION_LABEL`, uma entrada por indicador) dá a cada linha um ícone Lucide SEMANTICAMENTE ligado à sua ação, posicionado à ESQUERDA do texto (posição de liderança): `RotateCcw` (lastAccess, "Retomar atividade"), `Play` (progress, "Continuar sessão"), `MessageSquare` (sessions, "Fazer uma interação"), `Pencil` (reflections, "Registrar uma reflexão"), `Zap` (engagement, "Continuar agora"). Os 5 glifos são visualmente distintos entre si (seta circular ≠ triângulo ≠ balão ≠ lápis ≠ raio) e TODOS já pertencem ao vocabulário visual do app, escolhidos por grep dos ícones já importados em src (`RotateCcw` 37×, `Play` 40×, `MessageSquare` 36×, `Pencil` 16×, `Zap` 14×), reuso e não invenção de glifo novo. O `ArrowRight` menor e esmaecido (opacity-60) permanece ao final como reforço de affordance de clique. **(2) Botão parecido demais com o chip:** desde o Round 8 o botão e o chip "Como estou" ao lado ficaram ambos em pill tintado `/10`, competindo por serem iguais um ao outro (parte do "ta tudo muito igual"). Diferenciação incremental, sem reverter o tom `/10` do Round 8: o botão ganhou um ANEL sutil na cor do tom (`ring-1 ring-{tone}/25`, silhueta de elemento clicável que o chip não tem) e o peso da fonte subiu de `font-semibold` para `font-bold`. O chip segue só fundo `/10` sem anel; o olho passa a distinguir "ação" (contorno + bold + ícone de liderança) de "status" (chip liso). Novas props `Icon`/`iconTestid` no `ActionButton`; `data-testid` `action-icon-{key}` no ícone semântico para os testes afirmarem o glifo certo por linha (dupla evidência: o testid + a classe `lucide-<glifo>` que o próprio lucide-react emite). Nenhum token novo inventado (os `ring-semantic-*`/`ring-cerrado-600` derivam de tokens já no `@theme`); nenhuma alteração em `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`/célula Você/Turma; a estrutura de 2 colunas do Round 8 e as 2 linhas da Turma/Engajamento do Round 9 seguem intactas. typecheck exit 0; biome limpo; 278/278 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline era 270 antes da Round 10; +8: bloco Round 10 dedicado cobrindo o ícone certo por linha via testid, o glifo correto via classe lucide, os 5 glifos distintos entre si, o semântico liderando à esquerda com o ArrowRight ao final, o anel presente no botão e ausente no chip, o font-bold do botão vs não-bold do chip, a preservação da relação de cor no anel, e label/href intactos). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 19) | **Ilustrações Noodle CANCELADAS, substituídas por UM ícone Lucide reativo**, a partir de feedback direto do Hugo olhando o app: *"cancela a ideia das illustrations, coloca só um ícone"*. **Leitura do pedido (confirmada pelo escopo, não pela literalidade isolada):** o Hugo cancelou a abordagem de ILUSTRAÇÃO CUSTOMIZADA (os 5 SVGs do pacote Noodle + o badge branco do Round 18), não o conceito de reagir ao desempenho — a tabela inteira já comunica os mesmos 5 tons com ícones Lucide simples (`TrendingUp`/`Minus`/`ArrowRight` no chip "Como estou"; `RotateCcw`/`Play`/`MessageSquare`/`Pencil`/`Zap` nos botões de ação). **Remoção:** os 5 SVGs em `apps/web/public/illustrations/` foram removidos (`git rm`); o badge branco (`bg-white p-2`) e o `<img>` saíram do `RitmoSummaryPanel`. **No lugar, UM ícone Lucide reflete `summaryToneOf`** — a função pura NÃO mudou (severity-first + override de #1, ver ritmo-summary.ts), só o QUE é renderizado por tom mudou, de SVG custom para ícone. **Vocabulário REUSADO, não inventado:** `TrendingUp` (win) e `Minus` (tie) são os MESMOS glifos que o chip "Como estou" (`LEITURA_CHIP`) já usa para esses tons — reuso literal. O chip colapsa os dois graus de "atrás" num único `ArrowRight` (diferenciados só por cor), mas este painel é o ÚNICO lugar que resume o desempenho GERAL do aluno, então os dois graus ganharam glifos PRÓPRIOS para um sinal mais claro à primeira vista: `AlertTriangle` (behind-mild) e `AlertCircle` (behind-severe) — ambos já usados dezenas de vezes no app (grep de reconhecimento ANTES de escolher, mesma disciplina "reuso, não invenção" do Round 10: `AlertTriangle` 45×, `AlertCircle` 7×, `Compass` 4× em `apps/web/src`), não glifos novos inventados para este painel. `Compass` cobre `none` (ainda descobrindo o caminho, coerente com o subtítulo "minha jornada" do Round 18). **Tratamento visual (critério de design próprio, priorizando simplicidade por ser correção, não exploração nova):** círculo pequeno (`h-14 w-14 rounded-full`) tintado na MESMA família de cor que o chip já usa por tom (`bg-semantic-success/10 text-semantic-success` para win, e análogos para os demais) — sem badge branco: um glifo Lucide é vetor de cor única controlado via `text-*`, ao contrário do line-art preto-sobre-transparente do Noodle, que precisava de fundo claro para não sumir no painel escuro. typecheck exit 0; biome limpo; 303/303 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 303 antes da Round 19, **delta 0** — os 5 testes de ilustração foram RE-APONTADOS para o ícone, nenhum caso novo, nenhuma cobertura perdida: 2 testes de estrutura do painel + 3 testes do glifo reativo por tom, agora asserindo a classe `lucide-*` emitida pelo próprio SVG e a ausência de `<img>`/`/illustrations/`, em vez de `src`/`data-tone` do asset SVG). Servidor dev confirmado de pé em `localhost:3002` (HTTP 307 na home; `/illustrations/ritmo-win.svg` agora HTTP 404, confirmando a remoção do asset). 2 arquivos de código/teste tocados + 5 assets removidos: `student-home-card.tsx`, `student-home-card.test.tsx`, `public/illustrations/ritmo-{win,tie,behind-mild,behind-severe,none}.svg` (removidos via `git rm`). NÃO toquei `summaryToneOf`/`leituraFor`/`winnerOf`/`comparison-insights-table.tsx`/estrutura da tabela — só o `RitmoSummaryPanel` (ilustração → ícone). | Dex (@dev) |
| 2026-07-18 (Round 18) | **Subtítulo do card + remoção do CTA do painel de resumo + 5 ilustrações Noodle reativas.** Três frentes, a maior parte em `student-home-card.tsx`/`ritmo-summary.ts` (não na tabela). **(1) Subtítulo:** o cabeçalho do card "Meu ritmo" trocou "Como estou em relação à turma nos últimos 30 dias." por **"Como estou na minha jornada"** (sem ponto final, conforme a captura). **(2) CTA removido do painel de resumo:** o painel escuro do `buildRitmoSummary` (introduzido no R2) tinha um botão "Continuar agora" no canto; o Hugo achou "esquisito" e o CTA duplicava o CTA por linha que TODA linha da tabela já tem desde R4/R6 — removido. **(3) Ilustração reativa (a decisão de maior julgamento):** 5 SVGs do pacote `Noodle-Illustrations` (continuous-line, assinatura da landing Academy v2) copiados para `apps/web/public/illustrations/ritmo-{tone}.svg`, mapeados 1:1 aos 5 tons que já governam a tabela inteira (`Leitura["tone"]`: win/tie/behind-mild/behind-severe/none) — reuso da taxonomia existente, sem inventar uma sexta. **Tom GERAL do painel (não por linha):** nova função pura `summaryToneOf` (em `ritmo-summary.ts`, reusa o mesmo `leituraFor` da tabela), SEVERITY-FIRST com override de #1: (1) `isTopEngagement` → win (pico celebratório), (2) qualquer linha behind-severe → behind-severe, (3) qualquer behind-mild → behind-mild, (4) win em algo sem nada atrás → win, (5) tudo empate → tie, (6) só sem-dado → none. Racional: o parágrafo já elogia E aponta a oportunidade, então uma ilustração refletindo a pior área (quando há) é coerente e honesta; o override de #1 mantém o pico celebratório. **Legibilidade:** os glifos Noodle são line-art PRETO sobre transparente, que sumiria no painel escuro; então a ilustração vive num "badge" branco arredondado (`bg-white`), lida como ilustração emoldurada intencional e garante o contraste. Docada onde o CTA estava (direita em telas largas, abaixo do texto em telas pequenas). Prop `showNextStep` do card virou no-op deprecado (mantido no tipo p/ não quebrar `student-comparison.tsx`). typecheck exit 0; biome limpo; 303/303 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 293 antes da Round 18; +10: `summaryToneOf` 7 casos de tom geral, ilustração certa por tom + caminho `/illustrations/` no card, CTA ausente do painel, subtítulo novo, resumo+ilustração só na Visão detalhada). Visual confirmado: os 5 SVGs servidos em `localhost:3002/illustrations/ritmo-*.svg` → HTTP 200 `image/svg+xml`. 4 arquivos de código/teste + 5 assets novos. NÃO toquei `winnerOf`/`leituraFor`/`behindSeverityOf`/`ValueCell`/`PctBar`/estrutura da tabela (só importei `leituraFor`/`Leitura` no `ritmo-summary.ts`). | Uma (@ux-design-expert) |
| 2026-07-18 (Round 17) | **No EMPATE, as DUAS células de valor (Eu + Turma) destacam em amarelo.** Feedback do Hugo reagindo ao pill amarelo que o Round 16 pôs na célula Você: *"nesse caso, coloca o amarelo nos dois pois estão empatados"*. **Lógica do Hugo:** em win/behind só UM lado venceu, então só ele destaca; mas no empate os dois têm o MESMO valor, então os DOIS merecem o mesmo destaque. **EXCEÇÃO deliberada e datada** à regra "a coluna Turma NUNCA destaca" que existe desde o Round 3 — a regra geral CONTINUA (win/behind: Turma neutra), só o empate REAL abre a exceção. **Implementação sem duplicar lógica:** computo `subjectPill = subjectPillFor(winner, leitura.tone)` UMA vez e derivo `isRealTie = subjectPill === "tie"`; a célula Você usa `subjectPill`, a Turma usa `isRealTie ? "tie" : null`. Reuso a MESMA fonte de verdade de "empate real vs sem dado" do Round 16 (não re-derivo o empate por valores brutos). Amarelo idêntico ao dos outros elementos da linha (`VALUE_PILL.tie` `/15`). Comentários "NUNCA destaca" da célula Turma atualizados para documentar a exceção. typecheck exit 0; biome limpo; 293/293 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 291 antes da Round 17; +2: empate real → AMBAS as células com pill `/15`; e a exceção é SÓ empate — em win a Turma segue neutra, regressão coberta; o teste "Turma nunca destaca" do Round 3 foi retitulado para deixar claro que cobre win/behind, não empate). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 16) | **Botão tie não renderizava amarelo (causa de BUILD) + destaque amarelo no valor Você em empate.** Feedback do Hugo com screenshot: o chip do Round 15 pegou o amarelo, mas o botão ao lado na MESMA linha continuava branco/outline. **Ponto 1 — CAUSA RAIZ investigada (não reaplicação):** confirmei primeiro que o código estava certo (`ACTION_TONE.tie` = literal completo `bg-semantic-warning/60`, sem caminho outline resquício). O problema era BUILD: o Tailwind v4 SÓ gera a regra CSS de `bg-semantic-warning/NN` se aquela string EXATA aparecer no scan do projeto; o `/60` que escolhi no Round 15 não era usado em NENHUM outro lugar do app, então a classe ia pro HTML SEM regra CSS → fundo transparente → botão branco/outline (o chip `/15` funcionava porque `/15` já é usado — skill-badge). **Prova:** `grep` no CSS gerado (`.next/static/css`) mostrava `/5 /10 /15 /20 /70` mas NÃO `/60`. **Correção:** botão tie → `bg-semantic-warning/70 text-black/70`, o par EXATO já usado em `analytics-dashboard.tsx` (logo garantido presente no CSS gerado — confirmei `/70` no chunk após rebuild). Também limpei `.next` (`rm -rf apps/web/.next`) e reiniciei o dev para eliminar cache stale. **Lição:** opacidade nova de token depende do scanner gerar a regra; preferir valores já presentes no CSS ou verificar o CSS gerado, não confiar que "a classe existe no HTML" = "renderiza". **Ponto 2 — destaque amarelo no valor Você em empate:** `subjectPillFor` só retornava pill para win (verde) ou behind (âmbar/vermelho); no empate caía em `null` (texto neutro), inconsistente — a leitura "no ritmo" ficou visível mas o VALOR "50%" que ela descreve não destacava. Estendido: retorna `"tie"` quando `winner === null` E `tone === "tie"` (empate REAL: ambos presentes e IGUAIS, distinto de `"none"`/dado ausente que segue sem pill), `VALUE_PILL.tie` = `bg-semantic-warning/15 text-semantic-warning` (o MESMO amarelo já calibrado do chip, não um terceiro tom). Agora na linha empatada VALOR + chip + botão ficam todos amarelos coerentes. typecheck exit 0; biome limpo; 291/291 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 290 antes da Round 16; +1: `subjectPillFor(null,"tie")` agora → `"tie"` (era null), e o teste do empate na célula Você invertido de "sem pill" para "pill amarelo /15"; button tie re-apontado de `/60` para `/70`). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 15) | **Correção do amarelo do empate, que estava ABAIXO DO LIMIAR DE PERCEPÇÃO**, feedback do Hugo com screenshot: *"cadê o amarelo?"* — o chip e o botão do empate (linha Progresso) apareciam praticamente BRANCOS/off-white, não amarelos. **Erro de processo do Round 14 (reconhecido):** escolhi `bg-semantic-warning/5` (chip) e `/40` (botão) confiando na matemática "opacidade menor = mais claro que behind-mild", sem verificar a cor PERCEBIDA contra o fundo BRANCO da tabela (`bg-card` oklch 1.0). O token warning é `oklch(0.8 0.15 70)`; a 5% de opacidade a chroma efetiva cai para ≈ 0.0075 (satProxy ≈ 0.031, abaixo do limiar ~0.05 de "near-white") — o olho via só branco sujo. **Verificação real desta vez (não só matemática de papel):** calculei a cor composta sobre branco para cada opacidade (`/5`→satProxy 0.031 near-white, `/15`→0.094 claramente colorido, `/60`→0.380 âmbar forte, `/100`→0.631 sólido) e confirmei o servidor renderizando em `localhost:3002`. **Correção mantendo a hierarquia:** CHIP `bg-semantic-warning/15 text-semantic-warning` (âmbar INEQUÍVOCO, valor já PROVADO legível no app — `skill-badge` "Reflexão", badge "PUT", ambos `/15`); BOTÃO `bg-semantic-warning/60 text-black/80` (âmbar claramente amarelo, satProxy 0.380, ainda mais suave que o sólido `/100` do behind-mild, satProxy 0.631). A distinção empate↔behind-mild continua: no BOTÃO pela opacidade (`/60` vs `/100`); no CHIP sobretudo pelo ÍCONE (`Minus` parity vs `ArrowRight` acionável) e copy — decisão consciente, já que ambos os chips são fill fraco e forçar o chip tie a ficar sob o `/10` do behind-mild o levaria de volta à invisibilidade. Demais 4 tons, magreza do Round 13, rótulos, ícones semânticos, testids e href intactos. typecheck exit 0; biome limpo; 290/290 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 290 antes da Round 15; delta 0 — os testes do tie foram RE-APONTADOS de `/5`/`/40` para `/15`/`/60`, valor corrigido sem novo caso; correção de valor, não nova superfície de cobertura). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 14) | **Empate (`tie`) em AMARELO CLARO/SUAVE, distinto do âmbar forte de `behind-mild`**, feedback do Hugo com screenshot da linha "Progresso - conclusão" (empate 50%=50%): *"ao invés de cinza, para os neutros vamos usar o amarelo"*. **Levantamento antes de implementar (Uma):** o âmbar já é a cor de `behind-mild` desde o Round 3 — perguntei ao Hugo (via coordenador) se o amarelo do empate deveria ser um tom diferente para não confundir as duas leituras. Ele confirmou: **amarelo mais claro/suave para `tie`, distinto do amarelo mais forte de `behind-mild`** — família amarela nos dois, visualmente distinguíveis. **Solução (uma única variável, não cor nova):** reusar o MESMO token `semantic-warning` em OPACIDADE MENOR. **Chip** (`LEITURA_CHIP.tie`): de `bg-black/5 text-text-secondary` (cinza) para `bg-semantic-warning/5 text-semantic-warning/70` — mais fraco que o `bg-semantic-warning/10 + texto pleno` do behind-mild. **Botão** (`ACTION_TONE.tie`): de `bg-bg-elevated + border` (neutro) para `bg-semantic-warning/40 text-black/70` — fundo âmbar PÁLIDO (~40%) vs o `bg-semantic-warning` SÓLIDO (100%) do behind-mild, texto preto (contraste WCAG). A distinção empate↔atrás-moderado é UMA variável — a opacidade do mesmo token (`/5` vs `/10` no chip, `/40` vs `/100` no botão) — mantendo a família coerente (ambos amarelos, um suave, outro forte). **Ícone `Minus` do chip tie preservado:** o pedido foi COR, não ícone; `Minus` (parity/no-change) continua a leitura semântica certa para empate, agora sobre âmbar suave. **Anel do botão tie removido** (`ring-1 ring-border-medium` era do Round 12 para distinguir a pill neutra do fundo; agora que o empate tem cor própria, o anel é redundante). Os outros 4 tons (win verde, behind-mild âmbar forte, behind-severe vermelho, none cerrado), a magreza do gestor (Round 13), rótulos, ícones semânticos, testids e href intactos. typecheck exit 0; biome limpo; 290/290 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 288 antes da Round 14; +2: teste do chip tie em âmbar suave `/5` distinto do `/10` do behind-mild, e teste de que tie e behind-mild são AMBOS amarelos mas em intensidades distintas — mesmo token, opacidade diferente; os testes que afirmavam o cinza/neutro do tie — `bg-black/5`, `bg-bg-elevated` — foram RE-APONTADOS para o âmbar suave, e o classificador de "5 tons distintos" ganhou a checagem `/40` ANTES do warning sólido para não confundir empate com behind-mild). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 13) | **Magreza do botão IDÊNTICA ao card do gestor (replicação exata, não interpretação)**, feedback do Hugo: *"os botões estão muito gordos, coloca exatamente do mesmo jeito que tá lá no gestor"*. O Round 12 acertou a cor sólida/saturada mas errou o TAMANHO. **Causa da "gordura" (diagnosticada lendo o código, não só o número):** o Round 12 forçava `h-8` (altura fixa 32px) + `justify-center` (centralização vertical forçada) + `font-bold`, engordando a pill verticalmente vs o `py-1.5` FLUIDO (~28px) do gestor. **Fonte replicada (leitura antes de mexer):** o botão de ação do card do gestor, `student-insights-table.tsx` linhas 854-899 (é literalmente de onde veio a imagem de referência do Round 12 — "No ritmo"/"Lembrar"/"Acionar"). A classe base agora é IDÊNTICA à do gestor: `inline-flex items-center gap-1.5 rounded-full px-3.5 py-1.5 text-xs font-semibold shadow-sm transition-all hover:brightness-110` — removidos `h-8`/`justify-center`/`whitespace-nowrap`/`duration-200`, `font-bold`→`font-semibold`; ícone `size={13}`→`size={14}` (o exato do gestor). **Acréscimos nossos justificados (não estão no gestor):** `shrink-0` (o botão vive numa coluna densa e não deve encolher) e o foco visível `focus-visible:ring-2` + `active:scale` (o gestor é `<button>`, o nosso é `<a>`/Link navegável, que precisa de indicador de foco por teclado — não regredir acessibilidade ao replicar). **Cor tratada como intocável (instrução explícita "só o TAMANHO/PESO muda"):** `ACTION_TONE` mantém o fundo sólido por tom do Round 12 (o `hover:brightness-110`, que no gestor mora na classe base, migrou de `ACTION_TONE` para a base aqui também). A cor `#10b981` hardcoded do gestor NÃO foi copiada: nosso win segue em `var(--color-semantic-success)` (token do `@theme`), porque o Hugo não reclamou da cor (só da gordura) e a instrução foi mudar só tamanho/peso; a família de 5 tons (o gestor só tem 3) permanece, pois é requisito ativo desde o Round 3/7. Os 5 rótulos específicos (Round 12), os 5 ícones semânticos (Round 10), o ArrowRight de affordance, href, testids e universalidade intactos — a réplica é do TAMANHO/PESO/FORMA, não da semântica dos ícones do gestor (que são de outro contexto de produto). typecheck exit 0; biome limpo; 288/288 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 286 antes da Round 13; +2: teste da magreza exata `px-3.5 py-1.5 text-xs font-semibold` sem `h-8`/`justify-center`/`font-bold`, e teste do ícone em `size={14}`; o teste de diferenciação botão↔chip do Round 12 foi re-apontado de `font-bold` para "fundo sólido vs tintado /10", já que ambos agora são font-semibold como no gestor). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. `student-insights-table.tsx` (card do gestor) só LIDO como referência, NÃO tocado. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 12) | **Botão de ação em PILL SÓLIDA SATURADA por tom**, a pedido direto do Hugo com screenshot de referência ("o que acha de usar outras cores? ou usa esse estilo de botão"). A referência mostrava a coluna AÇÃO com pills SÓLIDAS/SATURADAS (fundo cheio na cor do tom, texto branco em negrito, ícone forte à esquerda, formato `rounded-full`), peso visual bem mais forte que o outline do Round 11 e que o tintado /10 do Round 8. **Decisão de rótulos (confirmada pelo Hugo via coordenador):** manter os 5 RÓTULOS específicos por métrica que já temos (Retomar atividade, Continuar sessão, Fazer uma interação, Registrar uma reflexão, Continuar agora), adotar SÓ o peso/saturação da referência — não os rótulos genéricos por status ("Lembrar"/"No ritmo"/"Acionar") da imagem. **Reavaliação do precedente do Round 8 (que abandonou o sólido por monotonia):** no Round 8 os botões eram idênticos entre si (mesmo ArrowRight, sem rótulo diferenciado forte), então 4 fundos verdes viravam "tudo igual". Desde o Round 10 cada linha tem ícone semântico próprio (RotateCcw/Play/MessageSquare/Pencil/Zap) + rótulo específico — a monotonia que motivou o Round 8 já está quebrada por ícone + texto distintos por linha. O sólido volta com segurança; decisão consciente, precedente reavaliado à luz do que mudou, não ignorado. **Forma `rounded-full` (não `rounded-xl` do DS):** fiel à referência e coerente com o chip "Como estou" vizinho (que já é `rounded-full`). O Round 11 usou `buttonVariants` outline (rounded-xl) para resolver o desalinhamento; esse diagnóstico segue correto, mas o Hugo agora pede um peso/forma específicos que o outline não entrega, então a pill sólida rounded-full escrita à mão é a resposta. Os estados que o DS dava de graça no Round 11 (foco visível `focus-visible:ring-2`, active, transição) foram reproduzidos à mão para não regredir acessibilidade. **Contraste WCAG por tom (lightness dos tokens em `theme.css`):** win (success, oklch L 0.65), behind-severe (error, L 0.6), none (cerrado, L 0.64) → texto BRANCO; behind-mild (warning, oklch L 0.8, CLARO) → texto PRETO (`text-black/80`), mesmo cuidado já validado no app, nunca branco sobre âmbar; tie → neutro sólido do DS (`bg-bg-elevated` + border) com texto primário. **Família de cor INTACTA** (verde=win, âmbar/vermelho=atrás, neutro=empate, cerrado=fallback, lógica central de leitura desde o Round 3): mudou só a SATURAÇÃO (/10 → sólido), não a família. `ACTION_TONE` reescrito de tintado para sólido; `buttonVariants` (import do Round 11) removido por não ser mais usado. Relação cor↔tom (Round 7), universalidade (Round 6), ícone + affordance (Round 10), href e testids intactos. typecheck exit 0; biome limpo; 286/286 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 284 antes da Round 12; +2 líquido: novo teste de contraste WCAG por tom + novo teste de preservação dos rótulos específicos; os testes Round 7/8/10/11 que afirmavam classes de outline/tintado foram RE-APONTADOS para as classes de fundo sólido — cor↔tom, testids, href, ícone preservados). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 11) | **Botão de ação ADOTA o design system real do app (mudança de abordagem, não mais ajuste de paleta na pill inventada).** Feedback do Hugo com screenshot da tabela: *"coloca os botões em outro estilo, não tá legal ainda"* — sem especificar QUAL estilo, só que o atual (Rounds 7→8→10 de ajuste de cor sobre a pill) não funciona. **Diagnóstico de design (Uma / @ux-design-expert):** depois de 3 rodadas de tweak de cor DENTRO da pill customizada, o botão ainda não convencia porque a causa era ESTRUTURAL, não de paleta. O `ActionButton` nasceu no Round 4 como uma PILL desenhada à mão (`rounded-full`, cor tintada por tom, reinventada rodada a rodada) e NUNCA usou o design system do app. O app inteiro fala `buttonVariants` de `@eximia/ui` (cva-based, dezenas de call sites: trails, assessments, workspace, brandbook, not-found) — inclusive o padrão IDÊNTICO ao nosso caso, `<Link href className={buttonVariants({ variant })}>` em `not-found.tsx`. O botão "não parecia certo" porque estava FORA da linguagem visual do resto do app. **Correção:** base = `buttonVariants({ variant: "outline", size: "sm" })` do DS (a variante outline é a certa p/ um CTA compacto e discreto numa célula de tabela densa: contorno + hover que revela a marca cerrado, sem competir com o pill de valor sólido); ganha os estados que a pill não tinha (foco visível `focus-visible:ring-2`, hover/active reais, `rounded-xl`, tipografia e transições da casa). **A RELAÇÃO cor↔tom do Round 7 é PRESERVADA** (requisito ativo do Hugo, não descartado): o tom da leitura tinge a base outline por cima via novo `ACTION_TONE` (cor do texto + do anel `ring-1` na família semântica do tom); chip e botão seguem a MESMA fonte de verdade (`leitura.tone`), coerentes, mas o botão é o Button do DS vestido pelo tom, não uma cápsula paralela. Universalidade (Round 6), ícone semântico + `iconTestid` (Round 10), `ArrowRight` de affordance, href/navegação e testids `action-*`/`action-icon-*` INTACTOS. `ACTION_BUTTON_STYLE` (a paleta da pill) SUBSTITUÍDO por `ACTION_TONE`; o rastro histórico dos Rounds 7/8/10 preservado no cabeçalho do arquivo. **Fix #2 (screenshot #2):** a legenda da célula Turma/Engajamento corrigida de "Média da turma: {N}" para "Média da turma: {N} pontos" (faltava a unidade), mesma fonte `reference.engagementAvg`. typecheck exit 0; biome limpo; 284/284 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline 278 antes da Round 11; +6, todos do novo bloco Round 11: forma do DS `rounded-xl`≠pill `rounded-full`, estados de foco/transição do DS, `<a>` navegável com href, relação cor↔tom sobrevive à mudança de base (5 tons → 5 cores de texto distintas), ícone semântico+ArrowRight dentro do botão do DS, e a unidade "pontos"; os testes Round 7/8/10 que afirmavam classes da pill foram RE-APONTADOS para as classes da base outline, sem perder cobertura de tom/testid/href). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Uma (@ux-design-expert) |
| 2026-07-18 (Round 9) | Célula Turma da linha Engajamento vira 2 LINHAS espelhando o lado Você, a partir de feedback direto do Hugo olhando o app rodando localmente com screenshot da célula que o Round 8 acabou de tocar. **Problema/pedido:** a célula Você da linha Engajamento já tem 2 linhas (pill de ranking "11º" em cima + legenda muted "Você fez {N} pontos" embaixo). O Hugo quis a MESMA estrutura do lado Turma: enquanto Você mostra a POSIÇÃO (11º), a Turma deve mostrar o TAMANHO da população (46 pessoas), para que juntas as duas células reconstruam "11 de 46" (o "de 46" que o Round 6 tinha removido do texto do rank, mas por um pedido ANTERIOR do Hugo, agora ele quer a informação de volta, só que do lado Turma em vez de colada no rank). **Correção:** a célula Turma da linha Engajamento passou de UMA frase ("a turma fez, em média, {N} pontos") para DUAS linhas. **(1) Linha de topo (valor principal, `text-sm font-medium text-text-muted`, mesmo peso das demais células Turma que o Round 8 fixou):** o total de pessoas via nova função pura `formatPopulation(indicators.subject.engagementTotalStudents)` (ex.: "15 pessoas"). O campo `engagementTotalStudents` é o MESMO que já alimenta `formatRank` (existe em `types/analytics.ts` e é populado por `student-home-indicators.ts` desde a Round 2), NENHUM cálculo novo. **(2) Linha de baixo (legenda muted `text-xs text-text-muted`, MESMO estilo da legenda "Você fez {N} pontos" do lado Você):** "Média da turma: {N}" (`reference.engagementAvg`, a MESMA fonte de sempre, só a redação mudou de "a turma fez, em média, X pontos" para "Média da turma: X"). **Degradação graciosa:** `formatPopulation` devolve `null` para total ausente/null/NaN/Infinity/<1 → a linha de topo é OMITIDA (nunca "undefined pessoas"), a legenda da média segue sozinha, mesmo espírito defensivo de `formatRank`/`formatFraction`. `data-testid` `cell-reference-engagement` migrou para o valor principal (linha de topo); a legenda da média ganhou `cell-reference-engagement-avg`. Nenhuma alteração em `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`/célula Você; a estrutura de 2 colunas chip/ação, o texto maior e o botão tintado do Round 8 seguem intactos. typecheck exit 0; biome limpo; 270/270 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline era 266 antes da Round 9; +4: `formatPopulation` puro (2 casos), o bloco da célula em 2 linhas (topo=total, baixo=média, degradação graciosa, isolamento por linha), e a supersessão do bloco Round 6 "frase única" reescrito para as 2 linhas). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 8) | Alinhamento em COLUNAS reais + hierarquia de cor + texto maior na frase da Turma, a partir de feedback direto do Hugo olhando o app rodando localmente com 2 screenshots (*"esse visual ta bem ruim, tudo muito igual, desalinhado e etc."* + apontando a frase "a turma fez, em média, 13 pontos": *"aqui coloque o tamanho maior de texto"*). Três causas raiz concretas, uma correção cada. **(1) Desalinhamento (estrutural):** o chip "Como estou" e o botão de ação viviam numa ÚNICA `<td>` com `<div className="flex flex-wrap">` — como o chip varia de largura entre linhas ("ativo acima da média" vs "no ritmo da turma"), o botão ao lado começava em X diferente por linha, parecendo desalinhado porque NÃO era coluna de verdade, era flex numa célula. Passaram a ser DUAS `<td>`s REAIS (chip \| ação): o `<thead>` ganhou uma 5ª `<th>` (rótulo `sr-only` "Ação") e cada `<tr>` tem 2 `<td>` no lugar de 1. Colunas nativas de `<table>` alinham automaticamente em todas as linhas (a ferramenta certa p/ "alinhar em coluna", não flexbox). `data-testid` `leitura-*`/`action-*` PRESERVADOS, só mudou o contêiner. **(2) Texto pequeno (explícito, screenshot 2):** a frase "a turma fez, em média, {N} pontos" (célula Turma da linha Engajamento) usava `text-xs text-text-muted` (12px, tamanho de legenda secundária), mas desde o Round 6 é o ÚNICO conteúdo primário daquela célula. Passou p/ `text-sm font-medium text-text-muted`, o MESMO peso tipográfico que `ValueCell` usa nas demais células Turma (`dim=true`), alinhando essa célula ao peso visual das outras da coluna, em vez de inventar um tamanho novo. **(3) Monotonia de cor ("tudo muito igual"):** num aluno vencendo, cada linha repetia VERDE SÓLIDO 3x (pill do valor Você win + chip win /10 + botão win sólido, decisão do Round 7) — três blocos fortes idênticos apagavam a hierarquia. `ACTION_BUTTON_STYLE` trocou de fundo SÓLIDO para TINTADO `/10` (mesma família do `LEITURA_CHIP`), texto na cor semântica sólida (não branco). Preserva a RELAÇÃO de cor por tom do Round 7 (o botão ainda espelha `leitura.tone`), mas com peso leve: agora só o PILL do valor (o dado numérico real) é sólido por linha; chip e botão ficam ambos suaves `/10` (mesma família entre si), criando hierarquia clara de 1 elemento forte (o número) + 2 leves de apoio (explicação + ação). NÃO reverte a decisão do Round 7 de "cor relativa ao Como estou" (pedido do Hugo na rodada anterior), só baixa o peso visual do botão. Nenhum token novo inventado (`bg-semantic-*/10` e `bg-cerrado-600/10` derivam de tokens já no `@theme`). Nenhuma alteração em `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`. typecheck exit 0; biome limpo; 266/266 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline era 259 antes da Round 8; +7: bloco Round 8 dedicado cobrindo estrutura de 2 colunas — chip e botão em `<td>`s separados, 5 `<th>` no cabeçalho, 5 `<td>` por linha, botão sempre na 5ª coluna —, texto maior da frase Turma/Engajamento, e o botão em tom suave `/10` preservando a relação de cor por tom; as asserções de `text-white`/`text-black/80` do bloco Round 7 foram atualizadas para o tom tintado). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |
| 2026-07-18 (Round 7) | Cor do botão acionável RELATIVA ao "Como estou", a partir de feedback direto do Hugo por áudio olhando o app rodando localmente: *"faça uma melhoria nos botões de ação e faça com que eles sejam relativos ao 'Como estou', de cores e relação."* **Problema:** desde a Round 6 o `ActionButton` era SEMPRE cerrado/laranja (`bg-cerrado-600`), uma cor fixa desconectada do status da linha — não havia relação visual entre o chip "Como estou" e o botão logo ao lado. **Correção:** a cor de FUNDO do botão passou a ESPELHAR o `leitura.tone` da MESMA linha, via nova constante `ACTION_BUTTON_STYLE: Record<Leitura["tone"], string>` (paralela a `LEITURA_CHIP`), indexada pelos 5 tons: **win** → verde sólido (`bg-semantic-success text-white`, CTA positivo de "continue assim"); **tie** → neutro (cinza/muted, mesma família do `LEITURA_CHIP.tie`); **behind-mild** → âmbar (`bg-semantic-warning text-black/80`, espelhando o chip mild — texto ESCURO porque o token warning é claro, oklch 0.8, reusando o par de contraste já validado em `analytics-dashboard.tsx`); **behind-severe** → vermelho sólido (`bg-semantic-error text-white`, espelhando o chip severe, forte para urgência real); **none** → o cerrado/laranja ORIGINAL preservado como fallback (dado ausente). O chip usa tons SUAVES (/10 de fundo + texto na cor semântica) e o botão usa tons SÓLIDOS (fundo cheio + texto de contraste) porque é um CTA, mais peso visual que o chip descritivo. Fonte única de verdade = `leitura.tone` (o mesmo que colore o chip), nenhuma conta paralela. Novo prop `tone: Leitura["tone"]` no `ActionButton` + `data-tone` para teste; texto/ícone/href/comportamento e a universalidade da Round 6 (botão nas 5 linhas sempre) PRESERVADOS — só a classe de cor (fundo + hover) muda por tom. Nenhum token novo inventado (`bg-semantic-*` e `cerrado-600` já existiam no `@theme`); nenhuma alteração em `winnerOf`/`leituraFor`/severidade (Round 3 intocada). typecheck exit 0; biome limpo; 259/259 testes verdes na suíte `src/components/analytics` + `src/lib/analytics` (baseline era 252 antes da Round 7; +7: o teste Round 6 "cor cerrado SEMPRE" foi REESCRITO para afirmar variação por tom, e um bloco Round 7 dedicado cobre os 5 tons + preservação de label/href + prova de 5 classes de fundo distintas). 2 arquivos tocados: `comparison-insights-table.tsx`, `comparison-insights-table.test.tsx`. | Dex (@dev) |

---

## Dev Agent Record

### Agent Model Used

Dex (@dev) — implementação concluída e verificada (wrap-up mecânico de handoff após queda de conexão do agente anterior; código já estava completo e correto, confirmado por revisão manual + typecheck + suíte do domínio).

### Debug Log / Decisões de Implementação

- **Shape do sinal de rank (AC7 / Nota @po ponto 1):** escolhido `isTopEngagement?: boolean` (booleano estrito) em vez de `engagementRank?: number`. Razão: é o mínimo necessário para desbloquear a copy "1º da turma", e evita vazar informação implícita (um `engagementRank: 3` permitiria, em orgs pequenas, inferência sobre colegas). LGPD: o cliente só recebe um booleano do PRÓPRIO aluno, nunca a lista ordenada nem scores alheios (AC13 preservado).
- **Rank real reusa a agregação existente (AC7 / Dev Notes "reuse, don't reinvent"):** o cálculo do rank vive dentro de `buildStudentHomeIndicators` sobre os MESMOS mapas de engajamento já construídos ali (`completedByStudent`, `reflectionsByStudent`), com a mesma fórmula `interactions*2 + reflections`. `area-gestor.ts` (`computeStudentComparison`) apenas propaga os denominadores por linha; NÃO duplica `engagementOf`.
- **Denominadores por linha ≠ `engagementMax` (R4):** `interactionsMax` (contagem crua de capítulos da trilha) e `reflectionsMax` (slides com reflexão possível) preservados como valores próprios, distintos da soma ponderada ×2/×1 de `engagementMax` (SH-F.5). Cada um alimenta a fração da sua linha; `engagementMax` continua alimentando só a soma.
- **Empate no topo (AC12):** `isTopEngagement` é estrito — 2+ alunos com o maior engajamento da org → NENHUM recebe `true`. Comentado no código para o @po endossar/ajustar na validação.
- **Local de `buildRitmoSummary` (Nota @po ponto 3):** módulo próprio `ritmo-summary.ts` (não `comparison-insights-table.tsx` nem `student-comparison-scale.ts`) — função pura, determinística, sem I/O/LLM/RNG; reusa `winnerOf` para nunca contradizer a leitura por linha; oportunidade de melhoria dinâmica via `behindMetricsOf` (nunca métrica hardcoded, AC9 cenário C).
- **Degradação graciosa (AC10):** `formatFraction(value, max)` retorna `"X/Y"` só com `max > 0`, senão o absoluto `"X"` — sem NaN, sem Infinity, sem crash (replica o precedente de `engagementMax` de SH-F.5). A coluna Turma nunca leva fração.
- **`buildRitmoSummary` renderizado só na "Visão detalhada" (`compareView === "table"`)** em `student-home-card.tsx`, fora da tabela, honrando a estrutura de SH-1.4 (o container é dono da renderização; a tabela permanece pure presentation).

#### Round 2 (refinamento visual, feedback ao vivo do Hugo)

- **Pills "Como estou" sem elipse de prefixo:** removido o "… " que abria cada frase da coluna "Como estou". O contrato de tom (win/tie/behind) e a leitura especial de rank real seguem idênticos; mudou só a apresentação da copy.
- **Fração nos dois lados (Interações/Reflexões):** a célula Turma dessas linhas passou a mostrar `{média}/{denominador}` com o MESMO denominador da trilha usado no lado Você (`interactionsMax`/`reflectionsMax`), em vez do valor absoluto que a Round 1 mantinha. `formatFraction` continua degradando gracioso ao absoluto quando o denominador é `0`/ausente (AC10 preservado).
- **Linha Engajamento assimétrica por decisão do Hugo:** a célula Você exibe RANKING "Xº de Y" (ranking de competição padrão, empate compartilha posição), a célula Turma exibe a fração de pontos "X/Y". A lógica de vencedor/cor/leitura da linha permanece sobre os scores brutos (`subject.engagement`/`reference.engagementAvg`) — apenas o texto renderizado das células mudou, sem tocar `winnerOf`/`leituraFor`. O sinal booleano `isTopEngagement` (AC7/AC12) segue sendo a única fonte da frase "1º da turma"; o novo texto de rank "Xº de Y" é derivado do mesmo rank real computado no backend, sem expor identidade/score de terceiros (AC13 preservado).
- **Parágrafo-resumo em painel de destaque + CTA local:** `buildRitmoSummary` passou de texto simples para um painel escuro em destaque, com o CTA "Continuar agora" ancorado no canto do próprio painel (sem o rótulo "Próximo passo: …"). O CTA é LOCAL ao painel do resumo — o `NextStepBar` compartilhado ficou 100% intocado nas outras 2 chamadas de `student-comparison-view.tsx`, evitando efeito colateral nos demais consumidores do componente compartilhado.

#### Round 3 (severidade + legibilidade, feedback ao vivo do Hugo)

- **Pontuação bruta sob o ranking (linha Engajamento):** a célula Você da linha "Engajamento" ganhou uma 2ª linha (legenda pequena, muted) "Você fez {N} pontos" logo abaixo do ranking "Xº de Y" da Round 2. É a pontuação bruta `subject.engagement`, exposta como legenda de apoio ao ranking (o ranking sozinho não dizia o valor absoluto). Adição EXCLUSIVA da célula Você da linha Engajamento — Turma e demais linhas intocadas. Nenhuma mudança na lógica de vencedor/cor: continua sobre os scores brutos.
- **Severidade amarelo/vermelho quando atrás (`behindSeverityOf`, `SEVERE_BEHIND_THRESHOLD = 0.3`):** antes, "atrás da média" tinha um tom único ("cerrado"/behind). Agora o chip "Como estou" e o pill de valor da célula Você graduam em amarelo (`mild`) ou vermelho (`severe`) conforme o quão atrás o aluno está. Nova função pura `behindSeverityOf`, **direction-aware** (respeita a direção de cada métrica — "higher is better" vs "lower is better", ex.: "Última atividade" em dias): só classifica severidade quando `winnerOf = "reference"` (aluno realmente atrás); o gap relativo `> SEVERE_BEHIND_THRESHOLD` (0.3, i.e. 30% pior que a média na direção certa) vira `severe` (vermelho), abaixo disso `mild` (amarelo). A Turma NUNCA destaca; empate/vitória continuam neutros. Fonte única da severidade é a mesma leitura (`winnerOf`/tom) que já governa a linha, sem inventar um segundo caminho de decisão.
- **Reversão explícita de "Jamais vermelho" (comentário histórico preservado):** o cabeçalho de `comparison-insights-table.tsx` documentava a decisão anterior de nunca usar vermelho (o "abaixo" era sempre neutro/acionável, nunca punitivo). Esta rodada REVERTE essa decisão por pedido direto do Hugo. O comentário histórico NÃO foi apagado — foi PRESERVADO e recebeu um bloco novo datado (2026-07-18, Round 3) explicando que a reversão é deliberada e sua origem. Isso mantém o rastro da decisão de produto (por que era "jamais vermelho" e por que deixou de ser), em vez de reescrever a história.
- **Threshold nomeado (referência futura):** `SEVERE_BEHIND_THRESHOLD = 0.3` — constante nomeada no topo do módulo, não número mágico inline. Calibrar aqui se o Hugo quiser mais/menos vermelho no futuro.

#### Round 4 (botão acionável por linha quando atrás, feedback ao vivo do Hugo)

- **Botão só quando atrás (mesma condição da severidade da Round 3):** o botão acionável compacto aparece ao lado do chip "Como estou" SOMENTE quando o aluno está atrás da média naquela linha (`winnerOf = "reference"`, severidade `mild` ou `severe`). Quando o aluno vence ou empata, o botão não é renderizado — o objetivo é oferecer o caminho de ação exatamente onde há oportunidade de melhoria, sem poluir as linhas onde o aluno já está bem. A condição reusa a MESMA `behindSeverityOf`/leitura por linha da Round 3, sem inventar um segundo caminho de decisão.
- **Label por linha (métrica-específico, ação clara):** "Retomar atividade" (Última atividade), "Continuar sessão" (Progresso), "Fazer uma interação" (Interações), "Registrar uma reflexão" (Reflexões), "Continuar agora" (Engajamento). O texto convida à ação concreta da métrica em que o aluno está atrás, coerente com o tom "convite nunca punitivo" já fixado no epic.
- **Deep-link único (decisão pragmática, Art. IV No Invention):** todos os botões apontam para o MESMO `continueHref` já existente no app. NÃO existe hoje deep-link para reflexão/interação/atividade específica — inventar um destino granular que não existe seria alegar uma capacidade fantasma. A decisão está documentada em comentário no código: quando o app ganhar deep-links por métrica, cada label passa a apontar para o alvo específico sem mudar a estrutura do botão. Por ora, o botão leva ao ponto de continuação geral, que é o comportamento honesto e útil disponível.
- **Prop aditivo seguro (`continueHref`):** novo prop opcional em `ComparisonInsightsTable`, com default seguro `DEFAULT_CONTINUE_HREF` para não quebrar os usos existentes em teste (mesmo padrão aditivo/opcional dos campos de SH-F.5). `student-home-card.tsx` passa o valor real do app (o `continueHref` já computado no container, reusado — não é um novo cálculo de rota). A tabela permanece pure presentation: recebe o href pronto, não decide para onde ir.

#### Round 5 (simetria de legenda de pontuação na linha Engajamento, feedback ao vivo do Hugo)

- **Legenda muted na célula Turma (simetria com a célula Você):** a célula Turma da linha "Engajamento" exibia só a fração crua de pontos ("13/57"), sem legenda em texto, enquanto a célula Você já trazia (desde a Round 3) a legenda muted "Você fez {N} pontos" abaixo do ranking. Adicionada uma 2ª linha equivalente na célula Turma: "Turma fez {N} pontos, em média", com o MESMO estilo visual (`text-xs text-text-muted`) da legenda Você, para que as duas células fiquem simétricas na leitura. A fração "X/Y" foi PRESERVADA — a legenda é aditiva, apenas coloca em texto o valor que a fração já mostra.
- **Fonte única, nenhum dado novo:** a legenda usa `indicators.reference.engagementAvg`, a MESMA fonte que já alimenta o numerador da fração "X/Y" nessa célula. Não há novo cálculo, nova prop, nem novo campo de tipo — só a renderização em texto de um valor que já estava presente. Adição EXCLUSIVA da célula Turma da linha Engajamento; célula Você e todas as demais linhas permanecem idênticas. Nenhuma mudança na lógica de vencedor/cor/leitura (segue sobre os scores brutos).

#### Round 6 (simplificações de legibilidade, feedback ao vivo do Hugo)

- **Engajamento/Turma sem denominador confuso:** a fração de pontos "{N}/57" na célula Turma da linha "Engajamento" gerou confusão real do Hugo ("o que é esse 57?") — o denominador de pontos possíveis da turma não tem leitura óbvia para o aluno. A célula passou a exibir só o absoluto `String(r.engagementAvg)` (média de pontos da turma), sem fração. O "57" (pontos possíveis) deixou de ser exibido; nada quebra porque a fração era só apresentação — a lógica de vencedor/cor/leitura já rodava sobre os scores brutos, não sobre a fração.
- **Botão acionável UNIVERSAL (reversão da condicionalidade da Round 4):** a Round 4 renderizava o `ActionButton` SÓ quando o aluno estava atrás da média naquela linha. O Hugo pediu explicitamente o inverso: "mesmo para o Rinaldo, tem que ter os botões para melhorar ainda mais a performance dele". Agora o botão aparece em TODAS as 5 linhas, sempre, como CTA de "continue melhorando" — o aluno que já vence a média também recebe o caminho de ação para evoluir mais. A condição de renderização (a checagem de severidade/`winnerOf = "reference"`) foi removida do gate do botão; label por linha e `continueHref` único permanecem os da Round 4. Isto NÃO reabre a severidade amarelo/vermelho da Round 3 (que continua condicional a "atrás da média") — só o botão passou a ser incondicional.
- **Ranking sem total (célula Você de Engajamento):** `formatRank` retornava "{rank}º de {total}" (ex.: "11º de 46"); o "de 46" confundia e não agregava leitura. Passou a retornar só "{rank}º" (ex.: "11º"). A posição sozinha comunica o ranking; o total da população deixou de ser exibido. O rank real (`isTopEngagement`/backend) e a frase "1º da turma – Parabéns!" (AC7) seguem intactos — mudou só a formatação do texto de posição, não a fonte do rank.
- **Turma/Engajamento consolidada numa única frase:** a célula Turma da linha Engajamento tinha, desde a Round 5, uma legenda muted em texto ("Turma fez {N} pontos, em média") logo abaixo de um número bruto solto. Os dois ficavam visualmente separados e o número duplicava o que a frase já dizia. Consolidada numa única linha de texto ("a turma fez, em média, {N} pontos"), removendo o número bruto solto acima — agora é só a frase, sem duplicação. Mesma fonte (`reference.engagementAvg`), nenhum dado novo. Adição/edição EXCLUSIVA da célula Turma da linha Engajamento; demais linhas intocadas.

#### Round 8 (alinhamento em colunas + hierarquia de cor + texto maior, feedback ao vivo do Hugo)

- **Diagnóstico por causa raiz (não achismo, leitura direta do componente):** o Hugo trouxe 2 críticas com 2 screenshots — "tudo muito igual, desalinhado" (geral) e "aqui coloque o tamanho maior de texto" (apontando a frase da Turma na linha Engajamento). Antes de tocar no código, mapeei 3 causas concretas: (1) o "desalinhado" era o botão de ação começando em X diferente por linha porque chip+botão viviam num `<div flex>` dentro de UMA `<td>`, e o chip varia de largura entre as linhas — não era uma coluna de verdade; (2) o "texto pequeno" era a frase "a turma fez, em média, {N} pontos" em `text-xs` (12px), tamanho de legenda secundária, sendo o único conteúdo primário da célula desde o Round 6; (3) o "tudo muito igual" era, num aluno vencendo, 3 blocos VERDE SÓLIDO idênticos por linha (pill do valor + chip /10 + botão sólido do Round 7).
- **(1) Colunas reais (estrutural, não hack de min-width):** o chip "Como estou" e o botão de ação saíram de 1 `<td>` com flex interno e viraram 2 `<td>`s REAIS. O `<thead>` ganhou uma 5ª `<th>` com rótulo `sr-only` "Ação" (o chip é a explicação visível, o botão é só o CTA, então o rótulo da coluna de ação fica acessível a leitores de tela sem poluir o visual). Cada `<tr>` passou de 4 para 5 `<td>`. Colunas nativas de `<table>` alinham automaticamente em todas as linhas — é a ferramenta correta p/ "alinhar em coluna", ao contrário de flexbox dentro de uma célula. Os `data-testid` `leitura-${row.key}` e `action-${row.key}` foram PRESERVADOS sem mudança de valor, só migraram de `<div>` dentro de 1 `<td>` para 2 `<td>`s separados.
- **(2) Texto maior na frase da Turma/Engajamento (peso das demais células Turma):** a classe da `<span>` da frase "a turma fez, em média, {N} pontos" subiu de `text-xs text-text-muted` para `text-sm font-medium text-text-muted` — o MESMO estilo tipográfico que `ValueCell` já usa nas outras células Turma (`dim=true`). Alinha essa célula ao peso visual das demais da coluna, em vez de inventar um tamanho novo. Mudança de classe pura, sem tocar conteúdo/fonte de dados.
- **(3) Botão em tom suave (reduz monotonia, PRESERVA a relação de cor do Round 7):** `ACTION_BUTTON_STYLE` trocou de fundo SÓLIDO para TINTADO `/10` (mesma família do `LEITURA_CHIP`), com texto na cor semântica sólida (não branco): `win: bg-semantic-success/10 text-semantic-success`, e análogos para `behind-mild` (warning), `behind-severe` (error), `none` (cerrado). `tie` já era neutro suave, ajustado para `/5` de contraste coerente. Isto NÃO reverte a decisão do Round 7 de "cor relativa ao Como estou" (o Hugo pediu explicitamente na rodada anterior) — o botão ainda espelha `leitura.tone` da linha; só o PESO visual baixou. O resultado é hierarquia clara: só o PILL do valor Você (dado numérico real) permanece em cor SÓLIDA por linha; chip e botão ficam ambos suaves `/10` (mesma família entre si) — 1 elemento forte (o número) + 2 leves de apoio (explicação + ação), em vez de 3 fortes competindo. Nenhum token novo (`/10` deriva dos `bg-semantic-*`/`cerrado-600` já no `@theme`).
- **Fora do escopo, intocado:** `winnerOf`, `leituraFor`, `behindSeverityOf`, `subjectPillFor`, `ValueCell`/pill do valor Você, `PctBar` — nenhum tocado (só apresentação da célula "Como estou"/"Ação"). Trabalho paralelo do Hugo (accept-invite, engagement/_components, analytics pages, migration/schema de report_name) não tocado, confirmado por `git status --short` pós-commit.

#### Round 9 (célula Turma/Engajamento em 2 linhas espelhando o lado Você, feedback ao vivo do Hugo)

- **Espelhar a estrutura que já existe do outro lado da MESMA linha:** a célula Você da linha Engajamento já tinha 2 linhas desde a Round 3 (o pill de ranking "11º" como valor principal + a legenda muted "Você fez {N} pontos" embaixo). O Hugo, olhando o screenshot da célula Turma que o Round 8 acabou de tocar, pediu a mesma gramática visual do lado Turma. A leitura semântica: a célula Você comunica a POSIÇÃO do aluno (11º), a célula Turma deve comunicar o TAMANHO da população (46 pessoas), e juntas reconstroem o "11 de 46" — o "de 46" que o Round 6 tirou do texto do rank (por um pedido anterior do próprio Hugo) volta agora, mas do lado Turma em vez de colado no rank. É uma reversão parcial e deliberada de uma decisão anterior, recolocada num lugar melhor.
- **Total de pessoas via `formatPopulation`, reusando o campo do `formatRank` (sem cálculo novo, Art. IV):** a linha de topo da célula Turma usa `formatPopulation(subject.engagementTotalStudents)`. O campo `engagementTotalStudents` já existe (`types/analytics.ts`) e já é populado no backend (`student-home-indicators.ts`, computado em `engagementRankOf` desde a Round 2) — é o MESMO total que `formatRank` recebe e valida. Nenhum novo dado, nenhuma nova prop de tipo, nenhum novo fetch; só uma nova função pura de FORMATAÇÃO (`formatPopulation`), no mesmo padrão defensivo de `formatFraction`/`formatRank`. Peso `text-sm font-medium` (das demais células Turma, herdado do Round 8).
- **"Média da turma: {N}" (mesma fonte, só a redação muda):** a linha de baixo é a legenda muted (`text-xs text-text-muted`, mesmo estilo da legenda "Você fez {N} pontos" do lado Você) com o texto EXATO pedido pelo Hugo, "Média da turma: {N}", onde N = `reference.engagementAvg` — a MESMA fonte que a frase anterior ("a turma fez, em média, X pontos") já usava. Só a redação do texto mudou, o dado é idêntico.
- **Degradação graciosa (mesmo espírito de `formatRank`/`formatFraction`):** `formatPopulation` devolve `null` para total ausente/`null`/`NaN`/`Infinity`/`<1`. Quando null, a linha de topo é OMITIDA condicionalmente (o `<div>` do valor principal, com o testid `cell-reference-engagement`, não é renderizado) — nunca "undefined pessoas" nem placeholder quebrado. A legenda "Média da turma: {N}" segue renderizada sozinha, garantindo que a célula nunca fica vazia mesmo sem o total. Testado com fixture `engagementTotalStudents: undefined`.
- **Migração de testid (documentada):** `cell-reference-engagement` (que no Round 6 identificava a frase única da célula) migrou para o valor principal, a linha de topo (o total de pessoas). A legenda da média ganhou testid próprio `cell-reference-engagement-avg`. O bloco de teste "Round 6 — frase única" foi reescrito para as 2 linhas (mantendo a asserção de que a frase antiga "a turma fez, em média, N pontos" não existe mais, agora como prova de supersessão). O `-raw` do Round 5 continua ausente.
- **Fora do escopo, intocado:** `winnerOf`, `leituraFor`, `behindSeverityOf`, `subjectPillFor`, `ValueCell`/pill, `PctBar`, a célula Você (as 2 linhas dela já existiam, não foram tocadas), a estrutura de 2 colunas chip/ação + texto maior + botão tintado do Round 8. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 10 (ícones semânticos por ação + diferenciação botão↔chip, feedback ao vivo do Hugo)

- **Reconnaissance de ícones ANTES de escolher (Dev Notes "reusar, não reinventar"):** antes de decidir os glifos, grep dos ícones Lucide já usados no app (`grep -rhoE` em `src/**/*.tsx`) para reusar o vocabulário visual existente. Resultado: `RotateCcw` (37×), `Play` (40×), `MessageSquare` (36×), `Pencil` (16×), `Zap` (14×) já são todos frequentes no app. Escolhi os 5 a partir dessa lista, priorizando semântica clara + distinção visual entre si, em vez de inventar glifos novos. As sugestões do pedido (`PlayCircle`/`NotebookPen`/`Flame`/`Rocket`) foram parcialmente ajustadas para os equivalentes JÁ presentes no vocabulário (`Play` em vez de `PlayCircle`, `Pencil` em vez de `NotebookPen`, `Zap` em vez de `Flame`/`Rocket`) — mesma intenção semântica, menor divergência do que o app já usa.
- **Mapa `ACTION_ICON` (paralelo a `ACTION_LABEL`):** `RotateCcw` (lastAccess, retomar), `Play` (progress, continuar sessão), `MessageSquare` (sessions, interação), `Pencil` (reflections, registrar reflexão), `Zap` (engagement, continuar agora). Uma entrada por `RowKey`, mesma mecânica de `ACTION_LABEL`. Os 5 são glifos genuinamente diferentes (seta circular / triângulo / balão / lápis / raio), verificado por teste que afirma `Set(glyphs).size === 5`.
- **Posição de liderança + affordance ao final:** o ícone semântico vai à ESQUERDA do texto (liderança visual, é o primeiro elemento que o olho encontra). O `ArrowRight` genérico, que antes era o único ícone (repetido nos 5), foi reduzido (size 11, `opacity-60`) e mantido ao FINAL como reforço de affordance de clique — não some, mas deixa de ser o protagonista. O ícone semântico leva `data-testid={iconTestid}` (`action-icon-{key}`) para asserção robusta.
- **Diferenciação botão↔chip (ajuste incremental, NÃO reforma):** o Round 8 baixou o botão para tintado `/10`, o que o aproximou visualmente do chip "Como estou" ao lado (também `/10`). Para o olho distinguir "ação clicável" de "status descritivo" sem reverter o `/10` (que resolveu a monotonia botão↔botão): o botão ganhou `ring-1 ring-{tone}/25` (contorno na cor do tom, que o chip não tem) e o peso subiu de `font-semibold` para `font-bold`. O anel PRESERVA a relação de cor por tom do Round 7 (win=success, severe=error, mild=warning, testado). O chip permanece liso, sem anel, no peso descritivo.
- **Asserção de ícone por dupla evidência (padrão do arquivo de teste):** os testes do chip já usavam `.querySelector("svg")` para provar presença de ícone. O Round 10 vai além: afirma (a) o `data-testid` `action-icon-{key}` existe e vive dentro do botão da linha, e (b) a classe `lucide-<glifo>` que o próprio `lucide-react` emite no SVG bate com o glifo esperado (ex.: `lucide-rotate-ccw` para lastAccess). Isso trava presença E correção semântica, não só "tem um ícone".
- **Fora do escopo, intocado:** `winnerOf`, `leituraFor`, `behindSeverityOf`, `subjectPillFor`, `ValueCell`/pill, `PctBar`, a célula Você, a célula Turma de outras linhas, a estrutura de 2 colunas do Round 8 e as 2 linhas da Turma/Engajamento do Round 9. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 11 (botão de ação adota o design system real, @ux-design-expert / Uma)

- **Hipótese validada antes de agir, não presumida:** o briefing trazia a hipótese "o botão não parece certo porque está fora da linguagem visual do app". Antes de aceitar, li o `packages/ui/src/components/button.tsx` inteiro (cva, variantes `outline`/`ghost`/`secondary`, estados de foco/hover/active) e fiz `grep` de `buttonVariants`/`@eximia/ui` em `apps/web/src`: dezenas de call sites (trails, assessments, workspace, brandbook, not-found), incluindo o padrão IDÊNTICO ao nosso — `<Link href className={buttonVariants({ variant })}>` em `not-found.tsx`. O `ActionButton` da tabela era o único que reinventava uma pill `rounded-full` à mão. Hipótese confirmada: a causa raiz do "não tá legal ainda" após 3 rodadas de ajuste de cor é desalinhamento com o DS, não falta de mais um tweak de paleta dentro da pill.
- **Escolha da variante `outline` (justificada, não default):** entre `outline`/`ghost`/`secondary`, `outline` é a mais adequada a um CTA compacto e discreto DENTRO de uma célula de tabela densa: o contorno dá silhueta de elemento clicável sem preencher um bloco de cor forte que competiria com o PILL de valor sólido da linha (a hierarquia "1 forte + 2 leves" do Round 8 é preservada — o número segue sendo o único elemento sólido). `size: "sm"` (`h-8 px-3.5 text-xs`) mantém a densidade do botão compacto anterior. `ghost` (sem borda) perderia a affordance de "clicável" que a diferenciação botão↔chip precisa; `secondary` (fundo cheio) reintroduziria o peso que o Round 8 removeu.
- **Cor↔tom (Round 7) NÃO descartada, re-expressa sobre o DS:** o briefing pediu explicitamente para não descartar a relação cor↔tom sem justificar — ela é requisito ativo. Em vez de uma pill de cor inventada, o novo `ACTION_TONE` tinge a base outline por cima: cor do texto + cor do anel (`ring-1 ring-{tone}/40`) na família semântica do tom (`win`=success, `tie`=neutro, `behind-mild`=warning, `behind-severe`=error, `none`=cerrado). Fonte única de verdade continua sendo `leitura.tone` (o mesmo que colore o chip), então chip e botão da linha seguem coerentes — mas agora o botão é o Button do DS vestido pelo tom, não uma cápsula paralela ao DS.
- **Diferenciação botão↔chip re-fundada no DS (substitui o font-bold do Round 10):** o Round 10 diferenciava por `font-bold` (botão) vs `font-semibold` (chip). A base `outline` do DS já é `font-semibold` e traz uma `border` real — a diferenciação passa a ser estrutural e nativa do DS (border + ring + estados de foco/hover) em vez de um peso de fonte inventado. O teste `font-bold` do Round 10 foi substituído por um teste de `border` presente no botão e ausente no chip liso.
- **Estados de acessibilidade ganhos de graça:** a pill inventada não tinha foco visível; a base do DS traz `focus-visible:ring-2 focus-visible:ring-cerrado-500/50 focus-visible:ring-offset-2`, `active:scale-[0.97]`, `disabled:opacity-40` e `transition-all`. WCAG: o CTA de navegação por teclado agora tem indicador de foco visível, que antes faltava. Continua sendo um `<a>`/`Link` navegável (é CTA de navegação, não `<button>`), href preservado.
- **Fix #2 (unidade "pontos"):** legenda da célula Turma/Engajamento de "Média da turma: {N}" para "Média da turma: {N} pontos". Mesma fonte `reference.engagementAvg`, sem tocar mais nada da célula. Comentários do Round 9 (topo + JSX inline) que citavam a redação antiga atualizados para manter o rastro coerente.
- **Testes re-apontados sem perda de cobertura:** os testes dos Rounds 7/8/10 afirmavam classes específicas da pill (`bg-semantic-*/10`, `bg-cerrado-600`, `ring-{tone}/25`, `rounded-full`, `font-bold`). Foram RE-APONTADOS para as classes da base outline + `ACTION_TONE` (`text-semantic-*`, `ring-{tone}/40`, `rounded-xl`, `border`), preservando a asserção de tom/testid/href/universalidade em cada um. +6 testes novos no bloco Round 11 dedicado (forma do DS, estados de foco, `<a>` navegável, cor↔tom sobrevive à base nova, ícone+affordance dentro do botão do DS, unidade "pontos"). Baseline 278 → 284 (delta +6, nenhuma cobertura perdida).
- **Fora do escopo, intocado:** `winnerOf`, `leituraFor`, `behindSeverityOf`, `subjectPillFor`, `ValueCell`/pill, `PctBar`, célula Você, estrutura de 2 colunas do Round 8, 2 linhas da Turma/Engajamento do Round 9, os 5 ícones semânticos + testids do Round 10. Trabalho paralelo do Hugo (accept-invite, engagement/_components, analytics pages, migration/schema de report_name) não tocado, confirmado por `git status --short` pós-commit.

#### Round 12 (pill sólida saturada por tom, @ux-design-expert / Uma)

- **Pedido do Hugo com referência visual:** o Hugo reagiu ao outline do Round 11 com um screenshot de referência (pills sólidas/saturadas, texto branco, ícone forte, `rounded-full`) e "usa esse estilo de botão". O coordenador confirmou, a pedido explícito do Hugo, manter os 5 rótulos específicos por métrica (não os genéricos por status "Lembrar"/"No ritmo"/"Acionar" da imagem), adotando só o peso/saturação.
- **Round 8 reavaliado, não ignorado (a parte importante do raciocínio):** o Round 8 abandonou o sólido por monotonia ("tudo igual" com 4 verdes). Mas naquele momento os botões eram idênticos entre si (mesmo ArrowRight, sem rótulo diferenciado). Desde o Round 10, cada linha tem ícone semântico próprio + rótulo específico — a monotonia já está quebrada por ícone + texto distintos por linha, mesmo com 4 fundos verdes. O risco que motivou o Round 8 caiu; o ganho de peso/clareza de CTA que o Hugo quer supera. Não repeti o Round 8 por reflexo: reavaliei com o que mudou.
- **Forma `rounded-full` à mão, não `buttonVariants` do DS:** o outline do DS (Round 11) é `rounded-xl` + border, que conflita com o fundo sólido saturado pedido. A referência do Hugo é pill `rounded-full`, e o chip "Como estou" vizinho já é `rounded-full` — a pill de ação `rounded-full` fica coerente com o chip e fiel à referência. Documentei a escolha: o diagnóstico do Round 11 (pertencer ao DS) segue correto, mas o Hugo agora pede um peso/forma específicos que o outline não entrega; a pill sólida é a resposta a esse pedido concreto.
- **Acessibilidade não regride:** ao sair do `buttonVariants`, os estados que o DS dava de graça (foco visível `focus-visible:ring-2 focus-visible:ring-offset-2`, `active:scale-[0.97]`, `transition-all`) foram reproduzidos à mão na classe base. O CTA de navegação por teclado mantém indicador de foco visível (WCAG).
- **Contraste WCAG decidido por lightness dos tokens (`theme.css`), não por chute:** li os tokens — `semantic-success` oklch L 0.65, `semantic-error` L 0.6, `cerrado-600` L 0.64 (escuros o bastante) → texto branco; `semantic-warning` L 0.8 (claro) → texto PRETO (`text-black/80`), o mesmo cuidado já documentado no arquivo e validado no app (`analytics-dashboard.tsx`), nunca branco sobre âmbar. `tie` → neutro sólido do DS (`bg-bg-elevated` + border) com texto primário, para o empate não gritar cor semântica mas ainda ter peso de pill.
- **Família de cor intacta, só a saturação muda:** verde=win, âmbar/vermelho=atrás, neutro=empate, cerrado=fallback — a lógica central de leitura do produto desde o Round 3 não se toca. `ACTION_TONE` só trocou de tintado `/10` para sólido 100%. `buttonVariants` (importado no Round 11) removido por não ser mais usado; `cn` permanece.
- **Testes re-apontados sem perda de cobertura:** os testes dos Rounds 7/8/10/11 afirmavam classes de outline/tintado (`text-semantic-*`, `ring-{tone}/40`, `rounded-xl`, `border`, `bg-semantic-*/10`). RE-APONTADOS para as classes de fundo sólido (`bg-semantic-*`, `text-white`/`text-black/80`, `rounded-full`, `shadow-sm`, `font-bold`), preservando tom/testid/href/ícone/universalidade em cada um. +2 testes líquidos no bloco Round 12 dedicado (contraste WCAG por tom: branco em fundo escuro, preto em âmbar claro; e preservação dos 5 rótulos específicos com ausência dos genéricos). Baseline 284 → 286 (delta +2, nenhuma cobertura perdida).
- **Fora do escopo, intocado:** `winnerOf`, `leituraFor`, `behindSeverityOf`, `subjectPillFor`, `ValueCell`/pill, `PctBar`, célula Você, estrutura de 2 colunas do Round 8, 2 linhas da Turma/Engajamento do Round 9, os 5 ícones semânticos + testids do Round 10, e a legenda "pontos" do Round 11. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 13 (magreza idêntica ao botão do gestor, @ux-design-expert / Uma)

- **Replicação exata, li a fonte antes de mexer:** o feedback foi literal ("do mesmo jeito que tá lá no gestor"), então não interpretei — li o botão de ação do card do gestor (`student-insights-table.tsx` linhas 854-899, a origem da imagem de referência do Round 12) e comparei lado a lado com o nosso `ActionButton` antes de editar.
- **Diagnóstico da "gordura" (a causa, não só o número):** o Round 12 tinha `h-8` (altura fixa 32px) + `justify-center` + `font-bold`. O gestor usa `py-1.5` FLUIDO (padding vertical que resulta em ~28px) + `font-semibold`, sem altura fixa. A altura forçada era a gordura; o `py-1.5` fluido enxuto é a magreza. Removi `h-8`/`justify-center`/`whitespace-nowrap`/`duration-200`, baixei `font-bold`→`font-semibold`, subi o ícone `size={13}`→`size={14}` (o valor exato do gestor). Classe base agora idêntica: `inline-flex items-center gap-1.5 rounded-full px-3.5 py-1.5 text-xs font-semibold shadow-sm transition-all hover:brightness-110`.
- **Acréscimos nossos justificados (não copiar cegamente = não perder o que temos de bom):** o gestor é `<button>` com `onClick`; o nosso é `<a>`/Link navegável. Um link precisa de indicador de foco por teclado, então mantive `focus-visible:ring-2` + offset + `active:scale-[0.97]` (que o gestor não tem) para não regredir acessibilidade ao replicar. `shrink-0` também é nosso (coluna densa). Documentei que esses 2 são acréscimos conscientes, não desvio da réplica.
- **Cor tratada como intocável (instrução "só o TAMANHO/PESO muda"):** `ACTION_TONE` mantém o fundo sólido por tom do Round 12. O `#10b981` hardcoded do gestor NÃO foi copiado — nosso win segue em `var(--color-semantic-success)` (token do `@theme`), porque o Hugo reclamou só da gordura, não da cor, e a instrução foi mudar tamanho/peso. Manter o token (em vez do hex do gestor) preserva a coerência de tema/dark-mode do nosso lado. A família de 5 tons (o gestor só tem 3: verde/âmbar/vermelho) permanece — é requisito ativo desde o Round 3/7, e replicar só 3 quebraria a leitura por tom. O único movimento de cor foi mover o `hover:brightness-110` de `ACTION_TONE` para a classe base (onde ele mora no gestor).
- **Testes re-apontados + 2 novos:** o teste de diferenciação botão↔chip do Round 12 afirmava `font-bold` no botão; re-apontado para "fundo sólido (botão) vs tintado /10 (chip)", já que ambos agora são `font-semibold` como no gestor. +2 testes: (a) magreza exata (`px-3.5 py-1.5 text-xs font-semibold` presentes, `h-8`/`justify-center`/`font-bold` ausentes), (b) ícone em `size={14}` via os atributos `width`/`height` do SVG que o lucide emite. Baseline 286 → 288 (delta +2, nenhuma cobertura perdida).
- **`student-insights-table.tsx` (card do gestor) só LIDO, NÃO tocado** — é referência, fora do escopo desta story. Fora do escopo também intocado: `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`/célula Você/estrutura do Round 8/2 linhas do Round 9/ícones+testids do Round 10/legenda "pontos" do Round 11. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 14 (empate em amarelo claro/suave, @ux-design-expert / Uma)

- **Levantei o risco antes de codar:** o pedido foi "para os neutros usa o amarelo", mas o amarelo já era a cor de `behind-mild` desde o Round 3. Em vez de aplicar cego (e criar duas leituras idênticas — "empate" e "atrás moderado" na mesma cor), sinalizei ao Hugo que precisavam ser tons distinguíveis. Ele confirmou: amarelo mais claro para o empate, mais forte para o behind-mild. Esse é o tipo de conflito que só aparece quando se conhece a paleta existente — não segui a instrução ao pé da letra sem checar a consequência sistêmica.
- **Distinção por UMA variável (o pedido explícito do coordenador):** reusei o MESMO token `semantic-warning`, só em opacidade menor. Chip: `/5` (tie) vs `/10` (behind-mild). Botão: `/40` (tie) vs `/100` sólido (behind-mild). Uma única dimensão de variação (opacidade) mantém a família amarela coerente e a distinção óbvia — em vez de inventar um segundo token/hex de amarelo, que fragmentaria a paleta.
- **Contraste WCAG no botão tie:** o fundo `bg-semantic-warning/40` é um âmbar pálido; texto `text-black/70` (dark sobre claro) mantém contraste. Deixei `/70` um degrau mais suave que o `text-black/80` do behind-mild, reforçando a hierarquia empate(suave) < atenção(forte) também no texto, não só no fundo.
- **Ícone `Minus` do chip tie mantido (decisão consciente, não omissão):** o pedido foi cor, não ícone. Avaliei se `Minus` (neutro) ainda combina com fundo amarelo — sim: `Minus` comunica "parity/sem mudança", que é exatamente a leitura de um empate ("no ritmo da turma"). Um ícone de alerta ali (ex.: triângulo) contradiria o tom "no ritmo". Mantido.
- **Anel do botão tie removido:** o `ring-1 ring-border-medium` existia no Round 12 só para dar silhueta à pill neutra sobre o fundo neutro. Agora que o empate tem cor de fundo própria (âmbar pálido), o anel virou redundante — removido para não sujar o visual.
- **Testes re-apontados + 2 novos:** os testes que travavam o cinza/neutro do tie (`bg-black/5` no chip, `bg-bg-elevated`+`ring` no botão) foram re-apontados para o âmbar suave. O classificador de "5 tons distintos" ganhou a checagem de `/40` ANTES da checagem de warning sólido — senão o empate (`bg-semantic-warning/40`) seria classificado como "warning" e colidiria com behind-mild. +2 testes: (a) chip tie em `/5` distinto do `/10` do behind-mild, (b) tie e behind-mild AMBOS amarelos mas em intensidades distintas (mesmo token, opacidade diferente). Baseline 288 → 290 (delta +2, nenhuma cobertura perdida).
- **Fora do escopo, intocado:** os outros 4 tons, `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`/célula Você, a magreza do Round 13, ícones/rótulos/testids/href. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 15 (o amarelo do empate estava invisível, @ux-design-expert / Uma)

- **Assumo o erro do Round 14 e a lição:** escolhi a opacidade do tie por raciocínio de "número menor = mais claro que behind-mild" e parei aí. Contra o fundo branco da tabela, `/5` (chip) e `/40` (botão) caíram abaixo do limiar de percepção — o Hugo, com razão, perguntou "cadê o amarelo?". Opacidade sobre fundo claro NÃO é uma comparação de números; é uma cor percebida que precisa ser verificada como cor, não como fração.
- **Verificação real desta vez (o que o coordenador exigiu):** computei a cor composta do token `oklch(0.8 0.15 70)` sobre branco para cada opacidade, com um proxy de saturação (max-min do RGB resultante): `/5` → satProxy 0.031 (near-white, confirmando o bug), `/10` → 0.063, `/15` → 0.094 (claramente colorido), `/40` → 0.251, `/60` → 0.380, `/100` → 0.631. O limiar prático de "não é mais branco sujo" fica em ~0.05; `/15` e `/60` passam com folga, `/5` não. Também subi o servidor dev em `localhost:3002` (estava caído entre rodadas) e confirmei que compila/serve.
- **Valores escolhidos com base em precedente do app, não chutados:** `/15 + text-semantic-warning` é exatamente o par que `skill-badge` ("Reflexão") e o badge "PUT" de `tenant-integrations` já usam como amarelo visível — não inventei um valor, reusei um já provado legível. O botão `/60` é o degrau que fica claramente amarelo (satProxy 0.38) sem chegar ao sólido `/100` do behind-mild (0.63).
- **Onde a hierarquia empate<behind-mild vive:** no BOTÃO é limpa (opacidade /60 vs /100 do mesmo token). No CHIP, decidi conscientemente NÃO forçar o tie a ficar sob o `/10` do behind-mild — a `/10` o próprio behind-mild já é fill fraco, e ir abaixo devolveria o empate à invisibilidade. No chip, a distinção empate↔behind-mild passa a ser o ÍCONE (`Minus` vs `ArrowRight`) e a copy, não a intensidade do fill. É a leitura correta: o chip é status (ícone+texto mandam), o botão é ação (a cor sólida manda).
- **Testes re-apontados (delta 0):** os asserts do tie migraram de `/5`/`/40`/`text-black/70` para `/15`/`/60`/`text-black/80`, incluindo o classificador de 5 tons (checa `/60` antes do warning sólido). Correção de valor, não nova cobertura — a suíte segue em 290.
- **Fora do escopo, intocado:** os outros 4 tons, `winnerOf`/`leituraFor`/`behindSeverityOf`/`subjectPillFor`/`ValueCell`/`PctBar`/célula Você, a magreza do Round 13, ícones/rótulos/testids/href. Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 16 (botão tie era bug de build + pill amarelo no valor Você, @ux-design-expert / Uma)

- **Investiguei a causa antes de reaplicar (o coordenador insistiu, com razão):** o botão continuava branco mesmo com o código do Round 15 aparentemente certo. Primeiro descartei "caminho outline resquício" — li o `ACTION_TONE` e o call site: literal completo `bg-semantic-warning/60`, `cn(base, ACTION_TONE[tone])`, sem outline sobrando. Então testei a hipótese de BUILD: `grep -rhoE "bg-semantic-warning/[0-9]+" .next/static/css` — apareciam `/5 /10 /15 /20 /70` mas NÃO `/60`. Diagnóstico confirmado: Tailwind v4 não gera a regra de uma opacidade que não é escrita como literal em algum lugar escaneado; o `/60` era único meu, ficou sem CSS, a classe no HTML apontava para regra inexistente → fundo transparente → "branco/outline". O chip `/15` renderizava porque `/15` existe no app (skill-badge).
- **Fix que GARANTE renderizar (não só "mais um valor"):** troquei o botão tie para `bg-semantic-warning/70 text-black/70` — o par EXATO de `analytics-dashboard.tsx`, portanto sempre gerado pelo scanner. Confirmei após rebuild que `/70` está no chunk CSS (2 chunks). Também limpei `.next` e reiniciei o dev (`localhost:3002` de volta em 307) para eliminar qualquer cache stale, como o coordenador pediu.
- **Lição registrada:** opacidade nova de token é um risco de build silencioso — a classe existe no HTML mas não no CSS, e o sintoma ("sem estilo") engana. Regra: reusar opacidades já presentes no CSS/app, ou verificar o CSS gerado, nunca declarar cor pronta sem confirmar o rendered.
- **Ponto 2 — pill amarelo no valor Você em empate, com o discriminador correto:** `subjectPillFor` ganhou o caso `winner === null && tone === "tie"` → `"tie"`. O ponto fino que o coordenador destacou: distinguir "empatou de verdade" de "sem dado". Aproveitei que `leituraFor` já faz isso — emite `tone: "tie"` só quando ambos os valores existem e são iguais, e `tone: "none"` quando falta dado. Então `tone === "tie"` É exatamente o empate real; reuso essa fonte de verdade em vez de re-derivar de valores brutos. `VALUE_PILL.tie` reusa o `/15` já calibrado do chip (não inventei um terceiro amarelo). `ValueCell` roteia `"tie"` pela mesma branch de pill tintado do behind.
- **Testes:** `subjectPillFor(null,"tie")` agora → `"tie"` (era `null` — teste invertido); o teste "empate continua neutro" da célula Você virou "empate destaca com pill /15"; botão tie re-apontado `/60`→`/70`. +1 líquido (291).
- **Fora do escopo, intocado:** os outros 4 tons, `winnerOf`/`leituraFor`/`behindSeverityOf`/`PctBar`, a magreza do Round 13, ícones/rótulos/testids/href, a célula Turma (que segue sem destaque neste round — Round 17 tratará o empate na Turma). Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 17 (no empate, as duas células destacam, @ux-design-expert / Uma)

- **Exceção deliberada e localizada, não reversão da regra:** a regra "Turma nunca destaca" existe desde o Round 3 por um bom motivo (em win/behind só um lado venceu, destacar os dois confundiria quem está à frente). O Hugo pediu a exceção SÓ para o empate, onde a lógica se inverte: os dois têm o mesmo valor, então destacar só um seria arbitrário. Tratei como exceção datada, com o comentário "NUNCA destaca" atualizado para registrar o porquê e o escopo — não apaguei a regra, documentei a fronteira dela.
- **Reuso da fonte de verdade, zero duplicação:** o ponto técnico que o coordenador pediu era não re-derivar "empate real" na Turma. Computei `subjectPill = subjectPillFor(winner, leitura.tone)` uma vez no map da linha e derivei `isRealTie = subjectPill === "tie"`. A célula Você usa `subjectPill`; a Turma usa `isRealTie ? "tie" : null`. Assim o discriminador "empate real vs sem dado" (que o Round 16 já resolveu dentro de `subjectPillFor`, via `tone === "tie"`) vale para os dois lados sem uma segunda cópia da condição — se a definição de empate mudar, muda num lugar só.
- **Mesmo amarelo, sem terceiro tom:** a Turma reusa `VALUE_PILL.tie` (`/15`), idêntico à célula Você e ao chip. Na linha empatada, valor Eu + valor Turma + chip + botão ficam todos na mesma família amarela.
- **Regressão coberta:** além do teste "empate → as duas células amarelas", adicionei "a exceção é SÓ empate: em win a Turma continua neutra", e retitulei o teste "Turma nunca destaca" do Round 3 para deixar explícito que ele cobre win/behind (não empate). +2 líquido (293).
- **Fora do escopo, intocado:** `winnerOf`/`leituraFor`/`behindSeverityOf`/`PctBar`, os outros tons, a magreza do Round 13, ícones/rótulos/testids/href. Servidor de pé em `localhost:3002` (307). Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 18 (subtítulo + CTA removido + 5 ilustrações reativas, @ux-design-expert / Uma)

- **Ordem: o simples primeiro.** Subtítulo trocado e CTA removido em minutos, antes de encarar a parte de julgamento (a ilustração reativa). Confirmei o texto exato do Hugo ("Como estou na minha jornada", sem ponto final) contra a captura.
- **A decisão de maior peso — qual tom governa o painel:** o painel é sobre o desempenho GERAL, não por linha. Investiguei `buildRitmoSummary`, que tinha 3 cenários de ABERTURA (rank #1 / acima da média / neutro) mas retornava só string, sem tom. Duas opções: mapear os 3 cenários de abertura, ou destilar um tom das 5 linhas. Escolhi **destilar das 5 linhas (severity-first + override de #1)** porque: (a) reusa o MESMO `leituraFor` da tabela, então a ilustração nunca contradiz o que as linhas mostram; (b) "pior tom quando há um" é coaching honesto — não celebrar com troféu quando o aluno está mal em algo; (c) o override de #1 preserva o pico celebratório real (o aluno literalmente mais engajado). Os 3 cenários de abertura são sobre engajamento; o tom geral é sobre o conjunto — decidi que o conjunto é a base certa da ilustração, com o #1 como exceção celebratória. Documentei o racional inteiro no bloco de `summaryToneOf`.
- **Taxonomia reusada, não inventada:** 5 ilustrações = os 5 tons já existentes (`Leitura["tone"]`). Validei a curadoria de candidatos do coordenador com meu julgamento — os 5 casam bem (win=troféu/à frente, tie=meta alcançada, behind-mild=lembrete/relógio, behind-severe=pressão/deadline, none=perdido/descobrindo o caminho) e adotei sem trocar. A amarração conceitual é sólida e evita uma sexta taxonomia no mesmo componente.
- **Problema de legibilidade resolvido no design, não ignorado:** inspecionei os SVGs — o line-art é PRETO sobre transparente (só os acentos coloridos `#f4d13b`/`#6966ed`/`#f953b2` teriam cor), o que sumiria no painel `bg-neutral-900`. Em vez de clarear o painel (fora de escopo) ou aceitar uma ilustração invisível, coloquei a ilustração num badge branco arredondado — que lê como moldura intencional E garante o contraste. Decisão de design consciente, documentada no componente.
- **Assets:** 5 SVGs do pacote Noodle copiados para `apps/web/public/illustrations/` com nomes limpos `ritmo-{tone}.svg` (padrão de `public/logos`). Verifiquei que renderizam: `curl localhost:3002/illustrations/ritmo-*.svg` → HTTP 200 `image/svg+xml` nos 5.
- **Prop deprecado sem quebrar call site:** `showNextStep` gatilhava o CTA removido. Não podia sumir (o `student-comparison.tsx` ainda passa), então virou no-op documentado no tipo, fora do destructure (biome não flagga prop não-destructurada). Limpeza do call site fica para depois.
- **Testes:** `summaryToneOf` (7 casos cobrindo a precedência inteira, incluindo #1-override-sobre-behind e all-tie e no-data); no card, ilustração certa por tom + caminho `/illustrations/`, CTA ausente do painel, subtítulo novo, resumo+ilustração só na Visão detalhada. +10 líquido (303).
- **Fora do escopo, intocado:** `winnerOf`/`leituraFor`/`behindSeverityOf`/`ValueCell`/`PctBar`/estrutura da tabela (só importei `leituraFor`/`Leitura` no `ritmo-summary.ts`, leitura, não alteração). Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

#### Round 19 (ilustrações canceladas → um ícone Lucide reativo, @dev / Dex)

- **Li os dois contratos ANTES de tocar código (Capataz exigiu):** o cabeçalho de `comparison-insights-table.tsx` (histórico completo das 17 rounds anteriores) e o Change Log + Dev Agent Record inteiro de `SH-1.5.story.md`, incluindo o racional do Round 18 que justificou a ilustração (`summaryToneOf`, o badge branco, a taxonomia de 5 tons). Não reverti nem reinventei nada que já estava decidido — só troquei o QUE renderiza por tom.
- **Interpretação do pedido, não literalidade isolada:** "cancela a ideia das illustrations, coloca só um ícone" tem duas leituras possíveis — cancelar só o MEIO (ilustração customizada) ou cancelar também o FIM (reagir ao desempenho). Optei pela primeira: a tabela inteira já reage ao desempenho via ícones Lucide (chip "Como estou", botões de ação); um painel de resumo mudo a esse respeito destoaria do resto do componente. `summaryToneOf` continua sendo consultado e continua governando o visual — só o glifo mudou.
- **Reuso de vocabulário, verificado por grep antes de escolher (mesma disciplina do Round 10):** `TrendingUp`/`Minus` são os MESMOS glifos que `LEITURA_CHIP` já usa para win/tie no chip "Como estou" — copiados 1:1, não escolhidos de novo. Para os dois graus de "atrás" (que o chip colapsa num único `ArrowRight` diferenciado só por cor), julguei que este painel — o ÚNICO resumo do desempenho GERAL — merece dois glifos distintos para o sinal ficar claro sem precisar ler a cor de perto; rodei `grep -rhoE` em `apps/web/src` ANTES de decidir e confirmei que `AlertTriangle` (45×), `AlertCircle` (7×) e `Compass` (4×) já pertencem ao vocabulário do app, então nenhum glifo novo foi introduzido.
- **Tratamento visual, critério próprio, simplicidade como prioridade explícita do briefing:** círculo `h-14 w-14 rounded-full` tintado na MESMA família de classe que `LEITURA_CHIP` já usa por tom (ex. `bg-semantic-success/10 text-semantic-success` para win) — decisão consciente de NÃO inventar uma nova paleta para o painel, e de não reproduzir o badge branco do Round 18 (que só existia por causa do line-art preto-sobre-transparente do Noodle; um glifo Lucide já é colorível via `text-*`, então o badge branco perderia a razão de existir).
- **Testes RE-APONTADOS, não descartados (delta 0, mesma disciplina de rounds anteriores como o Round 15/16):** os 5 testes do Round 18 (2 de estrutura do painel + 3 do glifo por tom) foram reescritos para o ícone — a asserção de `src`/`data-tone` do `<img>` virou asserção da classe `lucide-{glifo}` emitida pelo próprio SVG (`lucide-trending-up`, `lucide-circle-alert` — verificado o nome CANÔNICO do arquivo lucide-react 0.474.0 antes de escrever o teste, já que `AlertCircle` é alias de `circle-alert.js`, não `alert-circle.js`) + um teste novo confirmando a ausência de `<img>`/`/illustrations/` no DOM. Nenhum caso perdido, nenhum caso novo além da troca de sinal.
- **Verificação de runtime, não só de teste:** com o dev server já de pé (religado pelo coordenador), confirmei via `curl` que `/illustrations/ritmo-win.svg` responde `404` após a remoção (antes respondia `200 image/svg+xml`, provado no Round 18) — prova de que o asset saiu de verdade do `public/`, não só do código.
- **Fora do escopo, intocado:** `summaryToneOf`/`leituraFor`/`winnerOf`/`behindSeverityOf`/`ValueCell`/`PctBar`/toda a estrutura de `comparison-insights-table.tsx`, o subtítulo e a remoção do CTA (ambos do Round 18, permanecem). Trabalho paralelo do Hugo não tocado, confirmado por `git status --short` pós-commit.

### Comandos de Verificação Executados

```bash
cd apps/web && npx tsc --noEmit                 # exit 0 (typecheck limpo)
cd apps/web && npx biome check <arquivos>       # limpo nos arquivos tocados
cd apps/web && npx vitest run \
  src/lib/analytics/__tests__/ritmo-summary.test.ts \
  src/components/analytics/__tests__/comparison-insights-table.test.tsx \
  src/lib/analytics/__tests__/student-home-indicators.test.ts \
  src/lib/analytics/__tests__/area-gestor.test.ts
# 4 passed (4) · 102 passed (102): ritmo-summary 11, comparison-insights-table 29,
# student-home-indicators 37, area-gestor 25
```

Nota: as 23 falhas observadas ao rodar `pnpm --filter @eximia/web test -- <patterns>` NÃO são desta story — o `--` do pnpm não filtra por path e a suíte inteira roda; as falhas são de `src/app/api/sessions/[sessionId]/messages/route.test.ts` e outros domínios FORA do blast radius (mocks de `serviceClient`), pré-existentes/de outro trabalho em andamento, nenhum arquivo tocado por esta story. Os 4 arquivos de teste do domínio SH-1.5 estão 102/102 verdes quando rodados por path direto.

### File List

**Modificados:**
- `apps/web/src/components/analytics/comparison-insights-table.tsx` — `buildRows` 5 linhas reordenadas/renomeadas; linha Engajamento absoluta; `formatFraction` (fração graciosa Você-only); `LEITURA_COPY`/`leituraFor` estendidos com a leitura especial de rank real (`isTopEngagement`); header "Leitura" → "Como estou"; comentário de topo reescrito para o design real de 5 linhas. Round 3: 2ª linha muted "Você fez {N} pontos" na célula Você de Engajamento; nova função pura `behindSeverityOf` (direction-aware) + threshold `SEVERE_BEHIND_THRESHOLD = 0.3` graduando chip/pill Você em amarelo (mild)/vermelho (severe) quando atrás; reversão datada de "Jamais vermelho" no cabeçalho (comentário histórico preservado). Round 4: botão acionável compacto por linha ao lado do chip "Como estou", renderizado só quando atrás da média (mesma condição de severidade); label por linha; novo prop opcional `continueHref` (default `DEFAULT_CONTINUE_HREF`); deep-link único documentado em comentário. Round 5: 2ª linha muted "Turma fez {N} pontos, em média" na célula Turma da linha Engajamento (simetria com a legenda Você da Round 3), usando `reference.engagementAvg`, fração "X/Y" preservada. Round 6: célula Turma de Engajamento sem denominador (`String(r.engagementAvg)` absoluto, o "57" confuso removido) e consolidada numa única frase "a turma fez, em média, {N} pontos"; `ActionButton` UNIVERSAL (aparece em todas as 5 linhas sempre, condição de "só quando atrás" da Round 4 removida do gate); `formatRank` retorna só "{rank}º" (o "de {total}" removido). Round 7: `ACTION_BUTTON_STYLE` colore o botão por `leitura.tone` (relação chip↔botão). Round 8: chip e botão viraram DUAS `<td>`s reais (antes 1 `<td>` com flex interno) — `<thead>` com 5ª `<th>` `sr-only` "Ação", cada `<tr>` com 5 `<td>`, alinhamento nativo de `<table>`; frase da Turma/Engajamento subiu p/ `text-sm font-medium` (peso das demais células Turma); `ACTION_BUTTON_STYLE` baixou de sólido p/ tintado `/10` (hierarquia: só o pill do valor fica sólido), preservando a relação de cor por tom do Round 7. Round 9: célula Turma/Engajamento virou 2 linhas espelhando o lado Você — topo = total de pessoas via nova função pura `formatPopulation(subject.engagementTotalStudents)` (mesmo campo do `formatRank`, sem cálculo novo), baixo = legenda "Média da turma: {N}" (`reference.engagementAvg`, só a redação mudou); degradação graciosa omite a linha de topo se o total vier ausente (`formatPopulation` → null); testid `cell-reference-engagement` migrou para o valor principal + `cell-reference-engagement-avg` na legenda. Round 10: novo mapa `ACTION_ICON` (ícone semântico por linha, reusando `RotateCcw`/`Play`/`MessageSquare`/`Pencil`/`Zap` do vocabulário do app) à esquerda do texto (liderança), `ArrowRight` esmaecido ao final; `ActionButton` ganhou props `Icon`/`iconTestid` + `data-testid` `action-icon-{key}`; `ACTION_BUTTON_STYLE` ganhou `ring-1 ring-{tone}/25` e o botão subiu p/ `font-bold` (diferenciação botão↔chip, sem reverter o `/10` do Round 8).
- `apps/web/src/components/analytics/student-home-card.tsx` — renderiza `buildRitmoSummary` como parágrafo (`data-testid="ritmo-summary"`) abaixo da tabela, só na "Visão detalhada". Round 4: passa o `continueHref` real do app para `ComparisonInsightsTable`. Round 18: subtítulo → "Como estou na minha jornada"; `RitmoSummaryPanel` perdeu o CTA "Continuar agora" e ganhou a ilustração reativa (`RITMO_ILLUSTRATION` por tom, `data-testid="ritmo-illustration"`, num badge branco); `tone={summaryToneOf(indicators)}`; `showNextStep` deprecado (no-op, mantido no tipo). Round 19: `RITMO_ILLUSTRATION`/badge branco/`<img>` REMOVIDOS; novo mapa `RITMO_ICON` (um ícone Lucide por tom, `TrendingUp`/`Minus` reusados do `LEITURA_CHIP` da tabela, `AlertTriangle`/`AlertCircle`/`Compass` novos-mas-já-do-vocabulário-do-app) renderizado num círculo `h-14 w-14` tintado por tom, `data-testid="ritmo-illustration"` → `"ritmo-icon"`.
- `apps/web/src/lib/analytics/student-home-indicators.ts` — `buildStudentHomeIndicators` computa o rank real e propaga `interactionsMax`/`reflectionsMax`/`isTopEngagement` para `subject`.
- `apps/web/src/lib/analytics/area-gestor.ts` — `computeStudentComparison` preserva `interactionsMax`/`reflectionsMax` como valores próprios e os repassa a `buildStudentHomeIndicators`.
- `apps/web/src/types/analytics.ts` — `StudentHomeSubject` estendido com `interactionsMax?`, `reflectionsMax?`, `isTopEngagement?` (todos opcionais/aditivos).
- `apps/web/src/components/analytics/__tests__/comparison-insights-table.test.tsx` — testes AC1/AC2 (ordem+labels), AC3/AC4/AC10 (frações+degradação), AC5 (Engajamento absoluto), AC6/AC7 (Como estou + rank real, incluindo "acima da média mas não #1"), AC12; Round 2: fração nos dois lados, ranking "Xº de Y" na célula Você de Engajamento, pills sem elipse; Round 4: botão acionável presente por linha só quando atrás (label correto), ausente quando vence/empata, aponta para `continueHref`; Round 5: legenda "Turma fez {N} pontos, em média" presente na célula Turma da linha Engajamento; Round 6: botão acionável presente em TODAS as linhas (inclusive quando o aluno vence/empata), célula Turma de Engajamento sem o denominador "/57", ranking "Xº" sem "de {total}".
- `apps/web/src/components/analytics/__tests__/student-home-card.test.tsx` — Round 2: parágrafo-resumo em painel de destaque com CTA "Continuar agora" local ao painel; `NextStepBar` compartilhado intocado nas demais chamadas; Round 4: `continueHref` real propagado à tabela. Round 19: os 5 testes de ilustração do Round 18 RE-APONTADOS para o ícone (`ritmo-icon`, classe `lucide-*`, ausência de `<img>`/`/illustrations/`).
- `apps/web/src/lib/analytics/__tests__/student-home-indicators.test.ts` — propagação dos novos campos + rank real.
- `apps/web/src/lib/analytics/__tests__/area-gestor.test.ts` — rank real (AC7) + payload sem vazamento (AC13).

**Novos:**
- `apps/web/src/lib/analytics/ritmo-summary.ts` — `buildRitmoSummary` (função pura determinística) + `behindMetricsOf` (oportunidade de melhoria dinâmica). Round 18: nova função pura `summaryToneOf` (tom geral severity-first + override de #1) + tipo `SummaryTone`, reusando `leituraFor` da tabela.
- `apps/web/src/lib/analytics/__tests__/ritmo-summary.test.ts` — AC8/AC9 (parágrafo, 3 cenários, igualdade exata). Round 18: 7 casos de `summaryToneOf`.
- `apps/web/public/illustrations/ritmo-win.svg`, `ritmo-tie.svg`, `ritmo-behind-mild.svg`, `ritmo-behind-severe.svg`, `ritmo-none.svg` (Round 18) — 5 ilustrações Noodle continuous-line, uma por tom, para o painel de resumo reativo. Copiadas do pacote `Noodle-Illustrations` do ecossistema. **REMOVIDAS no Round 19** (`git rm`) — ver seção "Removidos" abaixo.

**Removidos (Round 19):**
- `apps/web/public/illustrations/ritmo-win.svg`, `ritmo-tie.svg`, `ritmo-behind-mild.svg`, `ritmo-behind-severe.svg`, `ritmo-none.svg` — as 5 ilustrações Noodle do Round 18, canceladas por pedido direto do Hugo ("cancela a ideia das illustrations, coloca só um ícone"), substituídas por um ícone Lucide (`RITMO_ICON` em `student-home-card.tsx`).

---

## Nota para @po (validação pendente)

Esta story ainda não passou por validação do @po. Pontos que merecem atenção na validação, sinalizados pelo @sm:

1. **Shape exato do sinal de rank** (AC7): `isTopEngagement?: boolean` vs `engagementRank?: number` — a story deixa a decisão para o dev/@po, mas recomenda o booleano por ser o mínimo necessário (evita vazar informação implícita como "sou o 3º" que poderia, em orgs pequenas, permitir inferência sobre os colegas).
2. **Regra de desempate no topo** (AC12): a story define "ninguém vence em empate" como default, mas isso é uma decisão de produto que pode merecer confirmação explícita do Hugo/@po, dado que o mockup não cobre esse caso.
3. **Local de implementação de `buildRitmoSummary`** (Task 6): a story deixa aberto entre `comparison-insights-table.tsx` e `student-comparison-scale.ts` — o @po pode preferir cravar um dos dois para consistência com o padrão de `buildProgressHeadline`/`buildVerdict` já existentes em `-scale.ts`.
4. **Custo do scan de rank:** ordenar `orgStudentIds` inteiro por engajamento é O(n log n) em memória sobre dados já carregados no `OrgReference` (nenhum scan novo de DB) — mas vale o @po confirmar que não há uma organização anormalmente grande onde isso vire um problema de performance perceptível.
